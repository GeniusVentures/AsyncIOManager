---
phase: 04-build-purge-generic-example
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - example/FileExample.cpp
  - example/example_data.bin
  - example/CMakeLists.txt
  - example/MNNExample.cpp
  - MNNExample.cpp
  - CMakeLists.txt
  - build/CommonBuildParameters.cmake
  - build/Windows/CMakeLists.txt
  - build/Linux/CMakeLists.txt
  - build/OSX/CMakeLists.txt
  - cmake/common.cmake
autonomous: true
requirements: [MNN-01, MNN-06, BUILD-01]
must_haves:
  truths:
    - "`git grep -iI mnn -- CMakeLists.txt '*CMakeLists.txt' '*.cmake'` returns no matches (exit code 1) — every MNN reference is gone from root CMakeLists, example CMakeLists, all three platform wrappers, CommonBuildParameters.cmake, and cmake/ has no common.cmake (MNN-01, D-31)"
    - "The wrapper reconfigure (`cmake -S build/Windows -B build/Windows/Debug`) succeeds and the full build compiles `FileExample.cpp` — the build log contains no `MNNExample` target and no MNN package lookup"
    - "Running `FileExample.exe` (from `example/` CWD) loads `example_data.bin` via `FileManager::LoadASync` and saves the roundtrip to `example_output/example_data.bin` via `FileManager::SaveASync`, printing both byte counts (BUILD-01, D-27)"
    - "`example/FileExample.cpp` contains zero MNN, libp2p, bitswap, or soralog references — includes are only `<iostream>`, `<memory>`, `<string>`, and `\"FileManager.hpp\"` (MNN-06, D-25)"
    - "`example/MNNExample.cpp`, root `MNNExample.cpp`, and `cmake/common.cmake` no longer exist on disk"
    - "Full ctest suite green after the swap (same 9 entries as Phase 3, zero failures)"
  artifacts:
    - path: "example/FileExample.cpp"
      provides: "minimal file://-only load→save roundtrip example (~50-80 lines, two-phase ioc run/restart/run)"
      contains: "ioc->restart"
    - path: "example/example_data.bin"
      provides: "committed ~1KB varied-byte default input for the example (D-28)"
    - path: "example/CMakeLists.txt"
      provides: "super-build-only add_executable(FileExample FileExample.cpp) — no MNN_LIBS glob, include_directories(../include) kept verbatim (D-26, P4)"
      contains: "add_executable(FileExample FileExample.cpp)"
    - path: "CMakeLists.txt"
      provides: "(edited) find_package(MNN CONFIG REQUIRED) + include_directories(${MNN_INCLUDE_DIR}) deleted — lines 34-35"
    - path: "build/CommonBuildParameters.cmake"
      provides: "(edited) super-build MNN block (separator + '# Set config of MNN' + 4 set/include lines) deleted per D-31"
    - path: "build/Windows/CMakeLists.txt"
      provides: "(edited) SET(TESTAPP FileExample ) — the live install(TARGETS ${TESTAPP}) reference renamed (D1)"
      contains: "SET(TESTAPP FileExample"
    - path: "build/Linux/CMakeLists.txt"
      provides: "(edited) commented '#SET(TESTAPP MNNExample )' line deleted (line 54)"
    - path: "build/OSX/CMakeLists.txt"
      provides: "(edited) commented '#SET(TESTAPP MNNExample )' line deleted (line 70)"
    - path: "example/MNNExample.cpp"
      provides: "(deleted) 291-line libp2p/bitswap/soralog-scaffolded example, replaced wholesale per D-25"
    - path: "MNNExample.cpp"
      provides: "(deleted) stale root-level legacy copy — built by nothing, references a header Phase 1 already deleted"
    - path: "cmake/common.cmake"
      provides: "(deleted) dead 31-line legacy helper holding the last three MNN link/glob references (D2 recommendation)"
  key_links:
    - from: "build/Windows/CMakeLists.txt (SET(TESTAPP FileExample))"
      to: "example/CMakeLists.txt (add_executable(FileExample ...))"
      via: "install(TARGETS ${TESTAPP} ...) resolved at CMake generate time after CommonBuildParameters.cmake:327 adds example/ — the rename pair MUST land in the same commit or the wrapper configure fails (D1, P2)"
      pattern: "SET\\(TESTAPP FileExample"
    - from: "example/FileExample.cpp (main)"
      to: "FileManager::LoadASync + FileManager::SaveASync"
      via: "two-phase ioc->run() → ioc->restart() → ioc->run() roundtrip (D4 — SaveASync is NEVER chained inside the load finalcall); save URL 'file://example_output/' is a directory prefix ending in '/' (D3)"
      pattern: "ioc->restart"
---

<objective>
Purge MNN from the entire build system (MNN-01) and swap `MNNExample` for a minimal generic `FileExample` (BUILD-01, MNN-06): root `CMakeLists.txt` loses `find_package(MNN)` + `include_directories(${MNN_INCLUDE_DIR})`, the super-build loses its MNN block in `CommonBuildParameters.cmake` (D-31) and its dead cousin `cmake/common.cmake` (D2), all three platform wrappers drop their `TESTAPP MNNExample` references (the Windows one is a LIVE install target — D1), and the 291-line `example/MNNExample.cpp` (+ stale root copy) is replaced by a ~60-line `file://`-only load→save roundtrip (D-25/D-27/D-28) that compiles against the post-Phase-3 API using the two-phase `run()/restart()/run()` io_context pattern (D4).

Purpose: the build system stops dragging an ML framework nobody links, and a consumer can build and run an honest end-to-end demo of the library's two public APIs. Every edit is mechanical deletion or one-token rename — the only new machinery is one small source file — but the rename is a distributed transaction (example CMakeLists ↔ Windows wrapper TESTAPP) that must land atomically (P2/P3) and be proven by one reconfigure.
Output: new `example/FileExample.cpp` + `example/example_data.bin`, edited root/example/super-build/wrapper CMake files, deleted `example/MNNExample.cpp` / root `MNNExample.cpp` / `cmake/common.cmake`, green reconfigure+build+grep+ctest gate with a live smoke run, `04-01-SUMMARY.md`.
</objective>

## Phase Goal

**As a** downstream consumer of AsyncIOManager (e.g. SuperGenius), **I want to** configure, build, and run a minimal `file://` load-and-save example from a build system that carries zero MNN dependency, **so that** adopting the library no longer drags an ML framework I don't use into my build and I can see both public APIs working end-to-end.

*(Vertical slice: after this plan, `FileExample.exe` runs a real load→save roundtrip from an MNN-free build. Wave 2 (04-02) clears the remaining test-side MNN residue — `.mnn` fixtures + the `mnn://` test literal.)*

## Artifacts this phase-plan produces

New symbols (created by this plan — exclude from drift verification):
- CMake target: `FileExample` (defined in `example/CMakeLists.txt`, super-build-only per D-26 — built via `BUILD_EXAMPLES=ON` through `build/CommonBuildParameters.cmake:326-327`, NOT via a new root `add_subdirectory`)
- Source file: `example/FileExample.cpp` (~50-80 lines)
- Binary asset: `example/example_data.bin` (1024 bytes, byte[i] = (i·7+13) mod 256)
- Executable (build tree, untracked): `build/Windows/Debug/example/Debug/FileExample.exe`

Removed symbols (deleted by this plan):
- CMake target/name: `MNNExample` (from `example/CMakeLists.txt` and `build/Windows/CMakeLists.txt:85`)
- Files: `example/MNNExample.cpp`, `MNNExample.cpp` (root), `cmake/common.cmake`
- CMake variables: `MNN_INCLUDE_DIR`, `MNN_LIBRARY_DIR`, `MNN_LIBS` (glob), and the `MNNExample` value of `TESTAPP`

Unchanged on purpose (their residue is Wave 2's, 04-02): `test/1.mnn`, `test/2.mnn`, `test/fixture.bin`, `test/src/filemanager_test.cpp` (`SaveASync_MnnPrefixThrows`).

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-build-purge-generic-example/04-CONTEXT.md
@.planning/phases/04-build-purge-generic-example/04-RESEARCH.md
@.planning/phases/04-build-purge-generic-example/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create example/FileExample.cpp + example/example_data.bin (D-25/D-27/D-28)</name>
  <files>example/FileExample.cpp, example/example_data.bin</files>
  <read_first>
  - .planning/phases/04-build-purge-generic-example/04-RESEARCH.md (section "Code Examples" — the complete ~60-line FileExample.cpp reference skeleton; "Public API the new FileExample compiles against" — exact post-Phase-3 signatures; Discoveries D3 and D4 — save-URL directory-prefix semantics and the two-phase ioc pattern; "Pitfalls" P5 — CWD sensitivity of the default input)
  - .planning/phases/04-build-purge-generic-example/04-PATTERNS.md (section "`example/FileExample.cpp` (example app, async file I/O roundtrip)" — include list, the three-token LoadASync delta from the old example, the restart idiom with in-repo precedent `test/testutil/asio_helpers.hpp:60-68`, the save-URL idiom from `filemanager_test.cpp:174-177`)
  - include/FileManager.hpp (the exact public API: `GetInstance()`, `InitializeSingletons()`, `ResultType`, `FinalCallback`, `LoadASync(url, save, ioc, finalcall, savetype)`, `SaveASync(url, data, ioc, finalcall, save_location = nullptr)`)
  - example/MNNExample.cpp (lines 262-278 — the post-Phase-3 LoadASync call shape being carried over; lines 208-210 — the work-guard pattern being REPLACED; the include block — what NOT to carry over)
  </read_first>
  <action>
  Create `example/FileExample.cpp` (~50-80 lines, D-25 minimal `file://`-only) using the RESEARCH §Code Examples skeleton as the starting draft (output wording is executor discretion; the locked structure is not):

  (1) Includes exactly `<iostream>`, `<memory>`, `<string>`, and `"FileManager.hpp"` — nothing else. `boost::asio::io_context` arrives transitively via FileManager.hpp. ZERO libp2p/bitswap/soralog/MNN includes or symbols (D-25, MNN-06). No `setBitswap` call — `InitializeSingletons()` alone suffices for `file://` (proven by `filemanager_test`).

  (2) `main(argc, argv)`: `inputUrl` = `argc > 1 ? argv[1] : "file://example_data.bin"`; `saveUrl` = `argc > 2 ? argv[2] : "file://example_output/"` (both defaults are literals; usage comment documents CWD-relative resolution per P5).

  (3) `FileManager::GetInstance().InitializeSingletons();` then `auto ioc = std::make_shared<boost::asio::io_context>();`.

  (4) Phase 1 LOAD: declare stack `FileManager::ResultType loaded;` → `LoadASync( inputUrl, false, ioc, [&]( FileManager::ResultType result ){...}, "" )` — `save=false` per D-27 (the auto-save chain is deliberately NOT demoed; it stays covered by `filemanager_test`). The callback: on success print the entry name (`result.value()->first[0]`) and byte count (`result.value()->second[0].size()`) and assign `loaded = result;`; on failure print `result.error().message()` (errors arrive in the callback, never thrown — the old example lines 268-276 are the pattern). Then `ioc->run();` (returns when FileManager's outstanding-operations counter drains and calls `ioc->stop()`). Then `if ( !loaded.has_value() ) return 1;`.

  (5) Phase 2 SAVE: declare `FileManager::ResultType saved;` → `SaveASync( saveUrl, loaded, ioc, [&]( FileManager::ResultType result ){...} )` (4 args — `save_location` defaults to nullptr, omit it). Callback mirrors the load callback (byte count + `saved = result` on success, `error().message()` on failure). Then `ioc->restart();` — MANDATORY before the second run (asio requirement; Discovery D4) — then `ioc->run();`. Return `saved.has_value() ? 0 : 1;`.

  HARD CONSTRAINTS (each is a verified pitfall): do NOT call `SaveASync` inside the load's final callback — the counter zero-crossing already stopped the ioc and the write handler would be abandoned (D4); the save URL literal MUST end in `/` — `LocalFileSaver` concatenates the URL path with each entry's basename, so `file://example_output/` produces `example_output/example_data.bin` while a bare `file://example_output.bin` would concatenate garbage (D3); do NOT use a work guard + single run (a guard cannot rescue the abandoned-handler problem, D4).

  Then create `example/example_data.bin` (D-28): a one-shot PowerShell command writes exactly 1024 bytes with byte[i] = `( $i * 7 + 13 ) % 256` (deterministic ramp, non-uniform so byte counts are meaningful) via `[IO.File]::WriteAllBytes` — full recipe in RESEARCH §Code Examples "Fixture generation". The binary is committed directly; NO generator script, NO CMake-time generation.

  Commit: `feat(04-01): add FileExample source and example_data.bin (D-25/D-27/D-28)`.
  </action>
  <verify>
  <automated>Test-Path example/FileExample.cpp, example/example_data.bin → both True; (Get-Item example/example_data.bin).Length -eq 1024 → True; git grep -inE 'mnn|libp2p|bitswap|soralog' -- example/FileExample.cpp → exit 1 (no matches); git grep -c 'ioc->restart' -- example/FileExample.cpp → 1; git grep -cE 'LoadASync\(|SaveASync\(' -- example/FileExample.cpp → 2</automated>
  </verify>
  <acceptance_criteria>
  - `example/FileExample.cpp` exists on disk
  - `git grep -inE 'mnn|libp2p|bitswap|soralog' -- example/FileExample.cpp` returns no matches (exit 1)
  - `example/FileExample.cpp` includes only `<iostream>`, `<memory>`, `<string>`, `"FileManager.hpp"` (no other `#include` lines)
  - `example/FileExample.cpp` contains `InitializeSingletons()`, one `LoadASync(` call whose save argument is `false`, one `SaveASync(` call whose URL argument ends in `/` (the `"file://example_output/"` default), and exactly one `ioc->restart()` between two `ioc->run()` calls
  - `example/FileExample.cpp` contains no `setBitswap` and no `executor_work_guard`
  - `example/example_data.bin` exists with `Length -eq 1024` and non-uniform content (first 16 bytes are not all identical)
  </acceptance_criteria>
  <done>
  `example/FileExample.cpp` exists with only the four allowed includes, `InitializeSingletons()`, a `LoadASync(` with save=false, a `SaveASync(` with a `/`-terminated URL, exactly one `ioc->restart()` between two `ioc->run()` calls, and no setBitswap/work-guard/MNN/libp2p tokens; `example/example_data.bin` is 1024 varied bytes. Compile proof lands in Task 3's build.
  </done>
</task>

<task type="auto">
  <name>Task 2: Atomic MNN CMake purge + MNNExample→FileExample rename transaction (MNN-01, D-31, D1/D2)</name>
  <files>CMakeLists.txt, example/CMakeLists.txt, build/CommonBuildParameters.cmake, build/Windows/CMakeLists.txt, build/Linux/CMakeLists.txt, build/OSX/CMakeLists.txt, cmake/common.cmake, example/MNNExample.cpp, MNNExample.cpp</files>
  <read_first>
  - .planning/phases/04-build-purge-generic-example/04-PATTERNS.md (every section from "`example/CMakeLists.txt` (config, rename-only)" through "`cmake/common.cmake` (config — DELETE)" — verbatim before/after states for all six CMake touchpoints, including the P4 warning to keep `include_directories(../include)`)
  - .planning/phases/04-build-purge-generic-example/04-RESEARCH.md ("Verified Live State" table — line numbers for every edit; Discovery D1 — why the Windows TESTAPP rename is mandatory; Discovery D2 — why cmake/common.cmake is dead; Pitfalls P2/P3 — atomicity, P4 — the include_directories trap)
  - CMakeLists.txt (lines 33-37 — the two MNN lines with their untouched OpenSSL/GSL/fmt neighbors)
  - example/CMakeLists.txt (whole 7-line file)
  - build/CommonBuildParameters.cmake (lines 265-285 — the MNN block bounded by the xxhash and LIBSSH2 blocks)
  - build/Windows/CMakeLists.txt (lines 83-88), build/Linux/CMakeLists.txt (lines 52-56), build/OSX/CMakeLists.txt (lines 68-72)
  - cmake/common.cmake (whole 31-line dead file)
  </read_first>
  <action>
  NINE mechanical edits, committed together as ONE transaction (P3: one reconfigure proves the set; P2: the example-CMakeLists rename and the Windows TESTAPP rename must not be split):

  (1) Root `CMakeLists.txt`: delete exactly two lines — `find_package(MNN CONFIG REQUIRED)` (line 34) and `include_directories(${MNN_INCLUDE_DIR})` (line 35). No other change to the file.

  (2) `example/CMakeLists.txt` → exact target state (per PATTERNS): delete the `FILE(GLOB MNN_LIBS "${MNN_LIBRARY_DIR}/*")` line; rename the target/source tokens on the three remaining build lines — `add_executable(FileExample FileExample.cpp)`, `add_dependencies(FileExample AsyncIOManager)`, `target_link_libraries(FileExample PRIVATE AsyncIOManager spdlog::spdlog protobuf::libprotobuf)`. KEEP `include_directories(../include)` verbatim — in wrapper mode this per-subdirectory line is what makes `#include "FileManager.hpp"` resolve (P4). KEEP the `#add_subdirectory(application)` comment.

  (3) `build/CommonBuildParameters.cmake`: delete the whole MNN block at lines 273-278 — the `# --------------------------------------------------------` separator, the `# Set config of MNN` comment, and the four active lines (`set(MNN_INCLUDE_DIR "${_THIRDPARTY_BUILD_DIR}/MNN/include")`, `set(MNN_LIBRARY_DIR "${_THIRDPARTY_BUILD_DIR}/MNN/lib")`, `include_directories(${MNN_INCLUDE_DIR})`, `set(MNN_INCLUDE_DIR "${CMAKE_CURRENT_SOURCE_DIR}/../MNN/include")`) — per D-31. The xxhash block above and the LIBSSH2 block below remain untouched; the file stays a sequence of independent blocks (trivially valid for all three wrappers that include it).

  (4) `build/Windows/CMakeLists.txt` line 85: `SET(TESTAPP MNNExample )` → `SET(TESTAPP FileExample )` — MANDATORY in the same commit as (2): `install(TARGETS ${TESTAPP} ...)` at line 87 resolves at generate time and fails the wrapper configure if the names diverge (D1). Nothing else in the file changes.

  (5) `build/Linux/CMakeLists.txt` line 54: delete the commented `#SET(TESTAPP MNNExample )` line.

  (6) `build/OSX/CMakeLists.txt` line 70: delete the commented `#SET(TESTAPP MNNExample )` line. (The pre-existing `install(TARGETS ${TESTAPP} ...)` latent no-op on Linux/OSX — TESTAPP undefined there — is NOT this phase's concern.)

  (7) Delete `cmake/common.cmake` outright (D2 recommendation, planner-locked: dead 31-line legacy helper — repo-wide grep shows zero `include(common.cmake)` and zero callers of `add_fileloader_library`/`add_fileloader_executable`; holds the last three MNN glob/link references. Deletion matches D-31's "MNN becomes consumers' concern everywhere" rationale and pre-clears Phase 5's `*.cmake` surface).

  (8) Delete `example/MNNExample.cpp` (291 lines — replaced by Task 1's file per D-25, deleted outright not slimmed).

  (9) Delete root `MNNExample.cpp` (117-line stale legacy copy — built by nothing; doubly dead since it `#include "MNNLoader.hpp"`, a header Phase 1 deleted).

  Commit (single): `feat(04-01): purge MNN from build system, rename MNNExample to FileExample (MNN-01, D-31, D-2)`.
  </action>
  <verify>
  <automated>git grep -iI mnn -- CMakeLists.txt '*CMakeLists.txt' '*.cmake' → exit 1 (no matches); Test-Path example/MNNExample.cpp, MNNExample.cpp, cmake/common.cmake → all False; git grep -n 'add_executable(FileExample' -- example/CMakeLists.txt → 1 match; git grep -n 'SET(TESTAPP FileExample' -- build/Windows/CMakeLists.txt → 1 match; git grep -n 'include_directories(../include)' -- example/CMakeLists.txt → 1 match</automated>
  </verify>
  <acceptance_criteria>
  - `git grep -iI mnn -- CMakeLists.txt '*CMakeLists.txt' '*.cmake'` returns no matches (exit code 1)
  - `Test-Path example/MNNExample.cpp`, `Test-Path MNNExample.cpp`, `Test-Path cmake/common.cmake` all return False
  - Root `CMakeLists.txt` contains no `find_package(MNN` and no `MNN_INCLUDE_DIR` (the OpenSSL/GSL/fmt neighbor lines intact)
  - `example/CMakeLists.txt` contains `add_executable(FileExample FileExample.cpp)`, `add_dependencies(FileExample AsyncIOManager)`, `target_link_libraries(FileExample PRIVATE AsyncIOManager spdlog::spdlog protobuf::libprotobuf)`, and still contains `include_directories(../include)`; contains no `MNN_LIBS` and no `MNN_LIBRARY_DIR`
  - `build/Windows/CMakeLists.txt` contains `SET(TESTAPP FileExample` and still contains `install(TARGETS ${TESTAPP} DESTINATION "${BUILD_FILELOADER_DIR}/bin")`
  - `build/Linux/CMakeLists.txt` and `build/OSX/CMakeLists.txt` contain no `MNNExample` string
  - `build/CommonBuildParameters.cmake` contains no `MNN` string; the xxhash block above and the `# Set config of LIBSSH2` block below remain intact
  </acceptance_criteria>
  <done>
  All nine edits landed: recursive `git grep -iI mnn -- CMakeLists.txt '*CMakeLists.txt' '*.cmake'` empty; `example/MNNExample.cpp`, root `MNNExample.cpp`, `cmake/common.cmake` gone; `SET(TESTAPP FileExample` paired with `add_executable(FileExample FileExample.cpp)`; `include_directories(../include)` retained in example/CMakeLists.txt. Configure proof lands in Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wave-1 gate — reconfigure, build, grep, ctest, FileExample smoke run</name>
  <files>(none modified — build-tree verification; evidence recorded in 04-01-SUMMARY.md)</files>
  <read_first>
  - .planning/phases/04-build-purge-generic-example/04-RESEARCH.md (section "Code Examples → Verification loop" — the exact 5-step PowerShell command set proven in Phases 1-3; Pitfalls P5 — smoke-run CWD, P6 — UTF-16 CMakeCache (use Select-String if inspecting; never gate on cache contents), P7 — stale MNNExample.vcxproj is inert after sln regeneration)
  </read_first>
  <action>
  Run the proven verification loop from repo root (PowerShell) and capture outputs for the SUMMARY:

  (1) `cmake -S build/Windows -B build/Windows/Debug` → must exit 0. This proves the D1 rename pair resolves (`install(TARGETS FileExample)`) and the D-31 block deletion left `CommonBuildParameters.cmake` valid. If configure fails on `install TARGETS given target ... which does not exist`, the rename pair is split — fix per Task 2 before proceeding.

  (2) `cmake --build build/Windows/Debug --config Debug` → must exit 0. The compile log must show `FileExample.cpp` being compiled and contain NO `MNNExample` target and NO MNN package lookup. (Stale `build/Windows/Debug/example/MNNExample.vcxproj`/`MNNExample.dir/`/`Debug/MNNExample.exe` may linger untracked — inert after the .sln regenerates; optionally delete the three stale paths as hygiene, P7.)

  (3) Grep gate: `git grep -iI mnn -- CMakeLists.txt '*CMakeLists.txt' '*.cmake'` → must return nothing (native exit code 1 = PASS). This is the stronger recursive form (P1) — it covers the wrappers the roadmap's literal first-level glob would miss.

  (4) `ctest --test-dir build/Windows/Debug -C Debug --output-on-failure` → must report `100% tests passed, 0 tests failed` over the same 9 test entries as Phase 3. The rename touched no test source, so any failure is a regression, not residue.

  (5) SMOKE RUN (the only live proof of the D-27 roundtrip and RESEARCH assumption A2): `Set-Location example; & ..\build\Windows\Debug\example\Debug\FileExample.exe; Set-Location ..` → expect output lines reporting the load (`example_data.bin`, 1024 bytes) and the save (`example_output/example_data.bin`, 1024 bytes), exit code 0, and `example/example_output/example_data.bin` created on disk with Length 1024. The `example_output/` directory is untracked run output — leave or delete it; do NOT commit it. Record the exact console output in the SUMMARY (this is BUILD-01's "demonstrates" evidence). Failure path spot-check (optional, discretion): running with a nonexistent input argument prints the callback error and exits 1.

  Do not edit any tracked file in this task; the build tree is untracked.
  </action>
  <verify>
  <automated>cmake -S build/Windows -B build/Windows/Debug → exit 0; cmake --build build/Windows/Debug --config Debug → exit 0; git grep -iI mnn -- CMakeLists.txt '*CMakeLists.txt' '*.cmake' → exit 1; ctest --test-dir build/Windows/Debug -C Debug --output-on-failure → "100% tests passed, 0 tests failed"; Test-Path example\example_output\example_data.bin → True (after smoke run)</automated>
  </verify>
  <acceptance_criteria>
  - `cmake -S build/Windows -B build/Windows/Debug` exits 0 (rename pair proven, D-31 deletion clean)
  - `cmake --build build/Windows/Debug --config Debug` exits 0; build log compiles `FileExample.cpp` and mentions no `MNNExample`
  - `git grep -iI mnn -- CMakeLists.txt '*CMakeLists.txt' '*.cmake'` returns no matches (exit 1)
  - `ctest --test-dir build/Windows/Debug -C Debug --output-on-failure` reports `100% tests passed, 0 tests failed` (9 tests)
  - The smoke run of `FileExample.exe` from `example/` CWD exits 0, prints load and save byte counts (1024 each), and `example/example_output/example_data.bin` exists with Length 1024
  - The smoke-run console output is captured verbatim in `04-01-SUMMARY.md` (BUILD-01 evidence + A2 proof)
  </acceptance_criteria>
  <done>
  Reconfigure exit 0 (rename pair + D-31 proven), build exit 0 compiling FileExample.cpp with zero MNNExample, recursive CMake grep gate empty, ctest 9/9 green, and the live smoke run printed both 1024-byte counts with exit 0 leaving `example_output/example_data.bin` on disk — evidence captured in `04-01-SUMMARY.md`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure deletion of internal build config + one new demo consumer of the existing, already-shipped `FileManager` URL dispatcher. No new network, auth, input, or crypto surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | CMake build files (root, wrappers, CommonBuildParameters) | accept | Pure deletions of internal config; no new fetch, download, or code generation introduced. Net effect REDUCES supply-chain surface: the MNN dependency (and its prebuilt-binaries lookup) leaves the build entirely. |
| T-04-02 | Elevation/DoS | example/FileExample.cpp argv → FileManager dispatcher | accept | argv URLs flow into the same dispatcher the old example already used — behavior unchanged, and the example is a demo binary, not a service. Malformed input reaches the existing error path (callback failure, exit 1), demonstrated by the optional failure spot-check. |
| T-04-SC | Tampering | package installs | accept | Zero package installs this phase (no find_package additions, no registry downloads — RESEARCH §Package Legitimacy Audit: "None required"). No [ASSUMED]/[SUS] packages exist to gate. |
</threat_model>

<verification>
Wave-1 gate (Task 3) is the phase's primary mechanical proof: wrapper reconfigure exit 0 → full build exit 0 with `FileExample.cpp` compiled and zero `MNNExample` → recursive `git grep -iI mnn -- CMakeLists.txt '*CMakeLists.txt' '*.cmake'` empty → ctest 9/9 green → live smoke run proving the load→save roundtrip with 1024-byte counts on both legs and `example_output/example_data.bin` on disk. Roadmap criteria 1 (zero MNN in CMake) and 2 (generic example builds + demo works + stale root copy gone) are fully discharged by this plan; criteria 3 (fixtures) and the phase-final consolidated gate land in 04-02.
</verification>

<success_criteria>
- All Task 1-3 acceptance criteria hold at plan end
- `git grep -iI mnn -- CMakeLists.txt '*CMakeLists.txt' '*.cmake'` is empty; `example/` contains `FileExample.cpp` + `example_data.bin` and no `MNNExample.cpp`
- `build/Windows/Debug` builds clean; `FileExample.exe` roundtrips `example_data.bin` → `example_output/example_data.bin` (1024 bytes both legs, exit 0)
- Full suite green (9 ctest entries, 0 failures)
- MNN-01, MNN-06, BUILD-01 discharged; D-25/D-26/D-27/D-28/D-31 honored (D-26 verified negatively: root `CMakeLists.txt` gains no `add_subdirectory(example)`)
</success_criteria>

<output>
Create `.planning/phases/04-build-purge-generic-example/04-01-SUMMARY.md` when done (include the smoke-run console output verbatim and the grep-gate result).
</output>
