---
phase: 04-build-purge-generic-example
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - test/1.mnn
  - test/2.mnn
  - test/fixture.bin
  - test/src/filemanager_test.cpp
autonomous: true
requirements: [MNN-03]
must_haves:
  truths:
    - "`test/1.mnn` and `test/2.mnn` no longer exist on disk; a committed generic `test/fixture.bin` (~1KB varied bytes) exists in their place (MNN-03, D-29/D-30)"
    - "No `.mnn` file or `mnn` string exists anywhere under `test/` — `git grep -inE 'mnn' -- test/` returns no matches (the D-32 neutralization is complete)"
    - "`filemanager_test` still contains a renamed unknown-prefix dispatch test (e.g. `SaveASync_FooPrefixThrows`) asserting `std::range_error` on a neutral prefix — same EXPECT_THROW shape, same fixture, exactly two tokens changed (D-32)"
    - "Windows build + full suite green after the fixture swap and test rename (same 9 ctest entries, zero failures)"
    - "Phase-end consolidated gate passes: `git grep -iI mnn -- CMakeLists.txt '*CMakeLists.txt' '*.cmake'` AND `git grep -iI mnn -- include/ src/ test/ example/` both empty, reconfigure + build + ctest green (roadmap criteria 1-4 all TRUE at phase end)"
  artifacts:
    - path: "test/fixture.bin"
      provides: "committed ~1KB varied-byte general-purpose binary fixture under test/ (D-30) — available to any current or future test; today's tests don't consume it"
    - path: "test/1.mnn"
      provides: "(deleted) 1.1MB MNN model, referenced by zero test sources (D-29)"
    - path: "test/2.mnn"
      provides: "(deleted) 3.8MB MNN model, referenced by zero test sources (D-29)"
    - path: "test/src/filemanager_test.cpp"
      provides: "(edited) SaveASync_MnnPrefixThrows → SaveASync_FooPrefixThrows: URL \"mnn://some/path\" → \"foo://some/path\", test name renamed — nothing else in the file changes (D-32)"
      contains: "SaveASync_FooPrefixThrows"
  key_links:
    - from: "test/src/filemanager_test.cpp (SaveASync_FooPrefixThrows)"
      to: "FileManager::SaveASync unknown-prefix dispatch → std::range_error"
      via: "EXPECT_THROW on \"foo://some/path\" — deliberately a DIFFERENT neutral prefix from the adjacent SaveASync_UnregisteredPrefixThrows (\"unknown://\") so the two tests stay distinct data points (P8)"
      pattern: "foo://some/path"
---

<objective>
Clear the test-side MNN residue the Wave-1 purge deliberately left behind: delete the two orphaned `.mnn` model fixtures (~5MB, referenced by zero test sources — their only consumers were the two example .cpp files Wave 1 deleted) and commit a generic `test/fixture.bin` in their place (MNN-03, D-29/D-30); neutralize the `SaveASync_MnnPrefixThrows` test literal per D-32 (two-token swap: `"mnn://some/path"` → `"foo://some/path"`, test name loses the MNN substring — the dispatch contract stays covered, zero behavioral change). Then run the phase-end consolidated gate proving all four roadmap Phase-4 criteria TRUE at once.

Purpose: `test/` ends the phase carrying zero MNN anything — fixtures are generic binaries, the last `mnn` string in the tree's test sources disappears now rather than as a Phase-5 grep-gate surprise, and the repo drops ~5MB of dead model weight.
Output: deleted `test/1.mnn` + `test/2.mnn`, new `test/fixture.bin`, edited `test/src/filemanager_test.cpp`, green consolidated phase gate, `04-02-SUMMARY.md`.
</objective>

## Phase Goal

**As a** downstream consumer of AsyncIOManager (e.g. SuperGenius), **I want to** pull a repo whose test tree carries only generic binary fixtures and whose suite still proves the unknown-prefix dispatch contract, **so that** nothing in the library's own tree hints at an ML dependency the library doesn't have.

*(Refinement slice of the phase: Wave 1 delivered the MNN-free build + working `FileExample`; this plan finishes the fixture/test surface and stamps the phase gate.)*

## Artifacts this phase-plan produces

New symbols (created by this plan — exclude from drift verification):
- Binary asset: `test/fixture.bin` (1024 bytes; byte[i] = (i·11+29) mod 256 — distinct pattern from `example/example_data.bin` per the D-30 recipe, executor may use any non-uniform ~1KB content)
- Test name: `FileManagerIntegrationTest.SaveASync_FooPrefixThrows` in `test/src/filemanager_test.cpp` (renamed from `SaveASync_MnnPrefixThrows` — exact new name is executor discretion, constrained only to carry no `mnn` substring)

Removed symbols (deleted by this plan):
- Files: `test/1.mnn`, `test/2.mnn`
- Test name: `FileManagerIntegrationTest.SaveASync_MnnPrefixThrows`
- String literal: `"mnn://some/path"` (from `test/src/filemanager_test.cpp`)

Unchanged on purpose: every other test in `filemanager_test.cpp` (P8 — the edit is exactly two tokens), `test/src/CMakeLists.txt` (verified zero mnn references), all Wave-1 artifacts.

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-build-purge-generic-example/04-CONTEXT.md
@.planning/phases/04-build-purge-generic-example/04-RESEARCH.md
@.planning/phases/04-build-purge-generic-example/04-PATTERNS.md
@.planning/phases/04-build-purge-generic-example/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fixture swap — delete .mnn models, commit test/fixture.bin (MNN-03, D-29/D-30)</name>
  <files>test/1.mnn, test/2.mnn, test/fixture.bin</files>
  <read_first>
  - .planning/phases/04-build-purge-generic-example/04-RESEARCH.md ("Verified Live State" rows for `test/1.mnn` + `test/2.mnn` — zero-source-reference proof; "Code Examples → Fixture generation" — the one-shot PowerShell recipe; "Don't Hand-Roll" row "Binary fixture generation" — no generator scripts, no CMake-time generation per D-28)
  - test/src/CMakeLists.txt (confirm zero `.mnn`/mnn references — verified in research, re-confirm before deleting assets)
  </read_first>
  <action>
  (1) Re-confirm the safety precondition live: `git grep -inE '\.mnn|mnn' -- test/src/ test/testutil/ test/CMakeLists.txt test/src/CMakeLists.txt` → only the `filemanager_test.cpp` D-32 site (Task 2's edit) may appear; NO fixture references. Every existing test builds its input at runtime via `TempFile("arbitrary string content")` — loaders treat files as opaque byte blobs, so nothing consumes the models. If any unexpected `.mnn` reference appears, STOP and re-scope instead of deleting.

  (2) Delete `test/1.mnn` (1.1MB) and `test/2.mnn` (3.8MB) from disk and index (D-29).

  (3) Create `test/fixture.bin` (D-30): one-shot PowerShell writes exactly 1024 bytes with byte[i] = `( $i * 11 + 29 ) % 256` (distinct ramp from `example/example_data.bin`'s `( $i * 7 + 13 ) % 256`) via `[IO.File]::WriteAllBytes` — recipe in RESEARCH §Code Examples. Commit the binary directly; NO generator script, NO CMake-time generation. No test consumes it today (MNN-03's spirit: a generic fixture living under `test/` for any current or future test).

  Commit: `feat(04-02): replace .mnn model fixtures with generic test/fixture.bin (MNN-03)`.
  </action>
  <verify>
  <automated>Test-Path test/1.mnn, test/2.mnn → both False; Test-Path test/fixture.bin → True; (Get-Item test/fixture.bin).Length -eq 1024 → True; git grep -ilE '\.mnn' -- test/ → exit 1 (no file references .mnn)</automated>
  </verify>
  <acceptance_criteria>
  - `Test-Path test/1.mnn` and `Test-Path test/2.mnn` both return False
  - `test/fixture.bin` exists with Length exactly 1024 and non-uniform content (first 16 bytes not all identical)
  - `git grep -ilE '\.mnn' -- test/` returns no matches (exit 1)
  - No test source, test CMake, or testutil file references `fixture.bin` as a build dependency (it is a committed asset only — no `addtest`/`target_...` changes)
  </acceptance_criteria>
  <done>
  `test/1.mnn` and `test/2.mnn` deleted; `test/fixture.bin` (1024 varied bytes) committed; zero `.mnn` references under `test/`. Suite impact: none expected (no test consumed the models) — proven by Task 3's ctest run.
  </done>
</task>

<task type="auto">
  <name>Task 2: D-32 neutralization — SaveASync_MnnPrefixThrows → neutral prefix (two tokens)</name>
  <files>test/src/filemanager_test.cpp</files>
  <read_first>
  - test/src/filemanager_test.cpp (lines 106-133 — the adjacent `SaveASync_UnregisteredPrefixThrows` template at ~106 and the target `SaveASync_MnnPrefixThrows` at ~124-133)
  - .planning/phases/04-build-purge-generic-example/04-PATTERNS.md (section "`test/src/filemanager_test.cpp` (test, D-32 neutralization)" — exact before/after with the two-token diff; Shared Pattern "EXPECT_THROW dispatch-contract tests")
  - .planning/phases/04-build-purge-generic-example/04-RESEARCH.md (Pitfall P8 — keep it mechanical; the target-state snippet in "Code Examples → D-32 neutralization")
  </read_first>
  <action>
  Exactly TWO token changes inside `SaveASync_MnnPrefixThrows` (test/src/filemanager_test.cpp lines ~124-133), nothing else in the file:

  (1) Test name: `SaveASync_MnnPrefixThrows` → `SaveASync_FooPrefixThrows` (exact new name is discretion; hard constraint: no `mnn` substring).

  (2) URL literal: `"mnn://some/path"` → `"foo://some/path"` — MUST differ from the adjacent `SaveASync_UnregisteredPrefixThrows`'s `"unknown://some/path"` so the two tests remain distinct data points (P8).

  Everything else stays identical: same `TEST_F( FileManagerIntegrationTest, ... )` fixture, same `IOContextRunner runner;` + `makeSingleFileResult( "test.bin", "content" )` body, same `EXPECT_THROW( { FileManager::GetInstance().SaveASync( <url>, data, runner.ioc(), []( FileManager::ResultType ) {} ); }, std::range_error )` shape. Do NOT "improve" the test (P8): no fixture changes, no assertion-style changes, no restructuring. The dispatch contract (unknown prefix → `std::range_error` from `src/FileManager.cpp`) keeps double coverage beside the adjacent unknown:// test.

  Commit: `test(04-02): neutralize mnn:// prefix test to foo:// (D-32)`.
  </action>
  <verify>
  <automated>git grep -inE 'mnn' -- test/ → exit 1 (no matches anywhere under test/); git grep -n 'SaveASync_FooPrefixThrows' -- test/src/filemanager_test.cpp → 1 match; git grep -n 'foo://some/path' -- test/src/filemanager_test.cpp → 1 match; git grep -n 'unknown://some/path' -- test/src/filemanager_test.cpp → still ≥2 matches (adjacent tests untouched)</automated>
  </verify>
  <acceptance_criteria>
  - `git grep -inE 'mnn' -- test/` returns no matches (exit 1) — the phase's `test/` surface is MNN-free after Tasks 1+2
  - `test/src/filemanager_test.cpp` contains `SaveASync_FooPrefixThrows` exactly once and `foo://some/path` exactly once
  - `test/src/filemanager_test.cpp` still contains `SaveASync_UnregisteredPrefixThrows` with `"unknown://some/path"` and `LoadFile_UnregisteredPrefixThrows` / `SaveFile_UnregisteredPrefixThrows` — all adjacent dispatch tests untouched
  - The renamed test retains `EXPECT_THROW`, `std::range_error`, `makeSingleFileResult( "test.bin", "content" )`, and the no-op callback — same skeleton (P8)
  - Exactly two logical changes vs. the pre-edit test (name + URL literal)
  </acceptance_criteria>
  <done>
  `SaveASync_FooPrefixThrows` asserts `std::range_error` on `"foo://some/path"` with the identical skeleton; `git grep -inE 'mnn' -- test/` empty; adjacent unknown:// dispatch tests untouched. Compilation/pass proof pending Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: Phase-end consolidated gate — build, full ctest, dual grep, roadmap criteria check</name>
  <files>(none modified — verification; results recorded in 04-02-SUMMARY.md)</files>
  <read_first>
  - .planning/ROADMAP.md (Phase 4 section — the four success criteria this gate discharges)
  - .planning/phases/04-build-purge-generic-example/04-RESEARCH.md ("Code Examples → Verification loop" — the proven command set; Pitfall P6 — never gate on CMakeCache contents)
  - .planning/phases/04-build-purge-generic-example/04-01-SUMMARY.md (Wave-1 gate evidence — this gate re-runs the shared steps and adds the test/ grep)
  </read_first>
  <action>
  Run the phase-end gate from repo root (PowerShell) and record every output in the SUMMARY:

  (1) `cmake -S build/Windows -B build/Windows/Debug` → exit 0.

  (2) `cmake --build build/Windows/Debug --config Debug` → exit 0; log compiles `FileExample.cpp` and the renamed test compiles into `filemanager_test`.

  (3) `ctest --test-dir build/Windows/Debug -C Debug --output-on-failure` → `100% tests passed, 0 tests failed` (9 entries — including `filemanager_test` with the renamed `SaveASync_FooPrefixThrows` case; `--gtest_list_tests`-style inspection optional, the green run is the proof).

  (4) DUAL grep gate (the phase's MNN-free claim, scoped to this phase's surfaces — the repo-wide gate is Phase 5's):
      - `git grep -iI mnn -- CMakeLists.txt '*CMakeLists.txt' '*.cmake'` → empty (exit 1) [roadmap criterion 1, carried green from Wave 1]
      - `git grep -iI mnn -- include/ src/ test/ example/` → empty (exit 1) [criteria 2/3 residue check — Phase 1 scrubbed include/src, this phase cleaned example/ and test/]
      (README.md's stale build transcript is OUT of every gate's scope — deferred, do not touch.)

  (5) Roadmap criteria checklist for the SUMMARY — verify each of the four Phase-4 criteria TRUE, citing the command outputs above: (1) zero MNN in CMakeLists ✓; (2) `FileExample` builds + demo works + root `MNNExample.cpp` gone ✓ (Wave-1 smoke evidence); (3) `.mnn` fixtures replaced by `.bin`, none under `test/` ✓; (4) green suite with the MNN-free build ✓.

  No tracked file changes in this task.
  </action>
  <verify>
  <automated>cmake -S build/Windows -B build/Windows/Debug → exit 0; cmake --build build/Windows/Debug --config Debug → exit 0; ctest --test-dir build/Windows/Debug -C Debug --output-on-failure → "100% tests passed, 0 tests failed" (9 tests); git grep -iI mnn -- CMakeLists.txt '*CMakeLists.txt' '*.cmake' → exit 1; git grep -iI mnn -- include/ src/ test/ example/ → exit 1</automated>
  </verify>
  <acceptance_criteria>
  - Reconfigure and build both exit 0
  - `ctest` reports `100% tests passed, 0 tests failed` over the same 9 test entries as Phase 3
  - Both grep gates return no matches (native exit code 1 each)
  - `04-02-SUMMARY.md` records the four roadmap Phase-4 criteria as TRUE with the command output citations
  - (Phase-boundary note in SUMMARY: repo-wide `grep -ri mnn` including README/docs and the clean-tree build remain Phase 5's acceptance gate)
  </acceptance_criteria>
  <done>
  Phase-end gate green: reconfigure + build exit 0, ctest 9/9 passed including the renamed dispatch test, both grep gates (CMake surface + include/src/test/example) empty, and all four roadmap Phase-4 criteria recorded TRUE with command citations in `04-02-SUMMARY.md`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Fixture asset swap + a two-token test-literal rename inside the existing suite. No new code paths, inputs, or services. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Repudiation | test evidence (renamed dispatch test) | mitigate | The rename preserves the assertion strength 1:1 (same EXPECT_THROW + std::range_error, distinct prefix from the adjacent test — P8); the green ctest run is the evidence, recorded in the SUMMARY with the test list. |
| T-04-04 | Tampering | committed binary fixtures | accept | Deterministic locally-generated bytes (ramp patterns), committed directly; no downloads, no submodules, no executables — content is inert data verified by Length/content checks in Task 1. |
| T-04-SC | Tampering | package installs | accept | Zero installs this plan (RESEARCH §Package Legitimacy Audit: none required). |
</threat_model>

<verification>
Phase-end consolidated gate (Task 3) proves all four roadmap Phase-4 criteria in one pass: zero MNN across all CMakeLists + .cmake (recursive git grep), `FileExample` built and smoke-proven (Wave-1 evidence + rebuilt green here), `.mnn` fixtures gone with `test/fixture.bin` committed and `test/` fully MNN-string-free (D-29/D-30/D-32), and the full 9-test suite green on the MNN-free Windows build. Boundary note: README.md transcript and the clean-tree end-to-end build are Phase 5's gate, by design.
</verification>

<success_criteria>
- All Task 1-3 acceptance criteria hold at plan end
- `test/` contains `fixture.bin` and zero `.mnn` files; `git grep -inE 'mnn' -- test/` is empty
- `filemanager_test` carries `SaveASync_FooPrefixThrows` (no mnn substring) asserting `std::range_error` on `foo://some/path`, adjacent unknown:// tests untouched
- Dual grep gates empty; reconfigure + build + full ctest green
- MNN-03 discharged; D-29/D-30/D-32 honored; all four roadmap Phase-4 criteria recorded TRUE in `04-02-SUMMARY.md`
</success_criteria>

<output>
Create `.planning/phases/04-build-purge-generic-example/04-02-SUMMARY.md` when done (include the gate outputs and the roadmap criteria checklist).
</output>
