---
phase: 03-parser-layer-removal
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - include/FileLoader.hpp
  - include/FileManager.hpp
  - include/LocalFileLoader.hpp
  - include/HTTPLoader.hpp
  - include/IPFSLoader.hpp
  - include/SFTPLoader.hpp
  - include/WSLoader.hpp
  - include/HTTPCommon.hpp
  - include/IPFSCommon.hpp
  - include/SFTPCommon.hpp
  - include/WSCommon.hpp
  - src/FileManager.cpp
  - src/LocalFileLoader.cpp
  - src/HTTPLoader.cpp
  - src/HTTPCommon.cpp
  - src/IPFSLoader.cpp
  - src/IPFSCommon.cpp
  - src/SFTPLoader.cpp
  - src/SFTPCommon.cpp
  - src/WSLoader.cpp
  - src/WSCommon.cpp
  - test/src/localfile_loader_test.cpp
  - test/src/http_loader_test.cpp
  - test/src/ipfs_loader_test.cpp
  - test/src/ipfs_device_test.cpp
  - test/src/filemanager_test.cpp
  - example/MNNExample.cpp
  - .planning/phases/03-parser-layer-removal/03-02-SUMMARY.md
autonomous: true
requirements: [PARSE-02, PARSE-03, PARSE-04]
must_haves:
  truths:
    - "CompletionCallback everywhere is std::function<void(ioc, ResultType, bool save)> — in FileLoader.hpp, FileManager.hpp, all 5 loader headers, and all 4 device headers"
    - "FileLoader::LoadASync pure virtual and all 5 overrides (LocalFile/HTTP/IPFS/SFTP/WS loaders) take (filename, bool save, ioc, callback); FileManager::LoadASync takes (url, bool save, ioc, finalcall, savetype)"
    - "Every call site — 5 test files and example/MNNExample.cpp — compiles and runs without a parse argument (D-22)"
    - "The D-19 auto-save test from Plan 01 still passes byte-for-byte after the strip (PARSE-03) and all other tests keep passing (PARSE-04)"
    - "Windows build green and full ctest green (9 tests, zero failures)"
    - "Phase-end gate (D-23/D-24) passes: word-boundary grep over include/ src/ test/ returns zero matches, including doc-comment @param scrub"
  artifacts:
    - path: "include/FileLoader.hpp"
      provides: "the type-coupling root stripped: CompletionCallback alias + LoadASync pure virtual without parse"
      contains: "bool                                     save"
    - path: "src/FileManager.cpp"
      provides: "4-param→3-param handle_read lambda; loader->LoadASync(filePath, save, ioc, handle_read) dispatch"
    - path: "example/MNNExample.cpp"
      provides: "the one example call site without its parse argument (D-22)"
      contains: "true,  // save"
    - path: ".planning/phases/03-parser-layer-removal/03-02-SUMMARY.md"
      provides: "phase-end gate evidence: grep outputs, build/ctest lines, SFTP mirror verification"
  key_links:
    - from: "include/FileLoader.hpp (base virtual)"
      to: "all 5 loader overrides + 4 device callback aliases + test lambdas + example call"
      via: "type-coupled signature change landing atomically in one commit"
      pattern: "CompletionCallback"
    - from: "D-23 gate"
      to: "include/ src/ test/ (minus vendored httplib.h)"
      via: "git grep word-boundary patterns with :(exclude)include/httplib.h"
      pattern: "bool\\s+parse\\b"
---

<objective>
Strip the `parse` bool from the entire async API surface in one atomic change (PARSE-02): the `FileLoader::LoadASync` pure virtual and its 5 overrides, the `CompletionCallback` alias in 9 declarations (FileLoader, FileManager, 5 loaders, 4 devices), the device-layer `parse_` members (HTTP/WS/SFTP) and IPFS's parameter-threaded `parse`, the ~24 posted-failure tails (`false, false` → `false`), all 5 test files' call sites plus ipfs_device_test's callback lambda definitions, and the one example call line (D-22). Then run the phase-end verification gate (D-23/D-24): word-boundary grep with the vendored-header exclusion, bare-`parse` eyeball check on the 4 capture-heavy files, Windows build, and full ctest — closing PARSE-03 (D-19 test still green) and PARSE-04 (no behavioral change beyond signatures).

Purpose: this is the compile-cascade big-bang (base virtual + 9 aliases type-couple every TU); it cannot land partially-compiling, so it is one plan, one commit-sized transaction, built on the green state Plan 01 left behind.
Output: 12 headers + 11 sources + 5 test files + 1 example line edited, `03-02-SUMMARY.md` with gate evidence.
</objective>

## Phase Goal

**As a** downstream consumer of AsyncIOManager (e.g. SuperGenius), **I want to** call `LoadASync(url, save, ioc, callback, savetype)` and receive `(ioc, ResultType, save)` callbacks with no parse concept anywhere in the API, **so that** my code compiles against an honest async I/O surface whose auto-save behavior is unchanged from the tested Plan 01 baseline.

*(Second vertical slice of the ROADMAP Phase 3 goal: the async signature strip + the enforcement gate; completes the phase.)*

## Artifacts this phase-plan produces

New symbols (created by this plan — exclude from drift verification):
- Post-strip canonical signature set (target state, not new code):
  - `FileLoader::CompletionCallback = std::function<void( std::shared_ptr<boost::asio::io_context> ioc, ResultType buffers, bool save )>` (identical shape re-declared in FileManager.hpp, LocalFileLoader.hpp, HTTPLoader.hpp, IPFSLoader.hpp, SFTPLoader.hpp, WSLoader.hpp, HTTPCommon.hpp, IPFSCommon.hpp, SFTPCommon.hpp, WSCommon.hpp — modulo each class's ResultType)
  - `FileLoader::LoadASync( std::string filename, bool save, std::shared_ptr<boost::asio::io_context> ioc, CompletionCallback callback )` pure virtual; same shape on all 5 loader overrides
  - `FileManager::LoadASync( const std::string &url, bool save, std::shared_ptr<boost::asio::io_context> ioc, FinalCallback finalcall, std::string savetype )`
  - Device ctors: `HTTPDevice(host, path, port, bool save)`, `WSDevice(ws_host, ws_path, ws_port, bool save)`, `SFTPDevice(..., bool save)` (parse was the 8th arg — drop it, keep save)
  - IPFS method defs: `StartFindingPeers(ioc, cid, filename, addressoffset, bool save, handle_read)`, same for `StartFindingPeersWithRetry`, `RequestBlockMain`, `convertUnixFSContentToResult`
- Artifact: `.planning/phases/03-parser-layer-removal/03-02-SUMMARY.md` (gate evidence)

Removed symbols (deleted by this plan):
- `bool parse` parameter from every signature above and from `FileManager::LoadASync`'s `handle_read` lambda
- Members `bool parse_;` from `HTTPDevice`, `WSDevice`, `SFTPDevice` and their ctor initializations (`parse_ = parse;` / `parse_(parse)` forms)
- The `false, // don't parse` argument line in `example/MNNExample.cpp` (D-22)
- Every `@param parse - ...` doc line (~15 blocks) and the `//We've read all the data, send to parse/save` comment at `src/SFTPCommon.cpp:392` (becomes `//We've read all the data` or similar — feeds the bare-parse eyeball check)

No new tests in this plan (D-19 landed in Plan 01; D-24 rejects a runtime parse-gate test — compile breakage is the guard).

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-parser-layer-removal/03-CONTEXT.md
@.planning/phases/03-parser-layer-removal/03-RESEARCH.md
@.planning/phases/03-parser-layer-removal/03-PATTERNS.md
@.planning/phases/03-parser-layer-removal/03-01-SUMMARY.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atomic parse-bool strip across headers, sources, tests, and the example (PARSE-02, D-22)</name>
  <files>include/FileLoader.hpp, include/FileManager.hpp, include/LocalFileLoader.hpp, include/HTTPLoader.hpp, include/IPFSLoader.hpp, include/SFTPLoader.hpp, include/WSLoader.hpp, include/HTTPCommon.hpp, include/IPFSCommon.hpp, include/SFTPCommon.hpp, include/WSCommon.hpp, src/FileManager.cpp, src/LocalFileLoader.cpp, src/HTTPLoader.cpp, src/HTTPCommon.cpp, src/IPFSLoader.cpp, src/IPFSCommon.cpp, src/SFTPCommon.cpp, src/SFTPLoader.cpp, src/WSLoader.cpp, src/WSCommon.cpp, test/src/localfile_loader_test.cpp, test/src/http_loader_test.cpp, test/src/ipfs_loader_test.cpp, test/src/ipfs_device_test.cpp, test/src/filemanager_test.cpp, example/MNNExample.cpp</files>
  <read_first>
  - .planning/phases/03-parser-layer-removal/03-PATTERNS.md (the whole file is the edit map: Pattern 1 FileLoader root, Pattern 2 loader headers, Pattern 3 loader impls incl. device-construction calls, Pattern 4 device impls, Pattern 5 IPFS threading, Pattern 9 test arity + ipfs_device lambda definitions, Pattern 10 example one-liner, "Rule of thumb" for the 24+ failure tails)
  - .planning/phases/03-parser-layer-removal/03-RESEARCH.md ("Verified Live Touchpoint Inventory" — per-file line anchors; Discovery 2 — SFTP not compiled, mirror mechanically; Pitfall 3 — ipfs_device_test lambda definitions; Pitfall 4 — IPFS [=] captures; Pitfall 7 — re-grep, don't navigate by line numbers)
  - include/FileLoader.hpp (the root: CompletionCallback ~29-31, LoadASync pure virtual ~48-53)
  - src/FileManager.cpp (the LoadASync wrapper after Plan 01: 4-param handle_read lambda, `loader->LoadASync( filePath, parse, save, ioc, handle_read )` dispatch)
  - src/WSCommon.cpp and src/HTTPCommon.cpp (canonical compiled device impls: ctor init, posted failures, success echo — the mechanical template for SFTP)
  - src/IPFSCommon.cpp (17 bare-parse sites enumerated in research: 4 method defs, explicit capture lists ~134/198/228/293/300, positional forwards)
  - test/src/localfile_loader_test.cpp (canonical test call-site shape, lines ~55-63 and ~90)
  - test/src/ipfs_device_test.cpp (callback lambda DEFINITIONS `(ioc, ResultType, bool, bool)` → 3 params, plus `StartFindingPeers[WithRetry](..., 0, false, false, callback)` → keep one false)
  - example/MNNExample.cpp lines 260-285 (the single LoadASync call — D-22)
  </read_first>
  <action>
  One atomic transaction, edited in this order to keep the cascade tractable (order is discretion; atomicity is not):

  (1) ROOT: `include/FileLoader.hpp` — drop `bool parse,` from the `CompletionCallback` alias and from the `LoadASync` pure virtual; delete the `@param parse - Whether to parse file upon completion` doc line in both doc blocks. Identical strip in `include/FileManager.hpp`'s alias and `LoadASync` decl (its `handle_read` lambda impl in the .cpp follows in step 3).

  (2) LOADER HEADERS (5): `LocalFileLoader.hpp`, `HTTPLoader.hpp`, `IPFSLoader.hpp`, `SFTPLoader.hpp`, `WSLoader.hpp` — each re-declares its own alias + override; drop `bool parse,` from both, scrub `@param parse` doc lines. Identical edit byte-for-byte modulo namespace/class name.

  (3) FILEMANAGER IMPL: `src/FileManager.cpp` — `FileManager::LoadASync` definition drops `bool parse` param; the `handle_read` lambda parameter list drops `bool parse` (3 params: ioc, buffers, save — keep the D-20 guard from Plan 01 exactly as-is inside); the dispatch becomes `loader->LoadASync( filePath, save, ioc, handle_read )`.

  (4) LOADER IMPLS (5): `LocalFileLoader.cpp`, `HTTPLoader.cpp`, `IPFSLoader.cpp`, `SFTPLoader.cpp`, `WSLoader.cpp` — per loader: override definition drops `bool parse,`; posted-failure tails `handle_read( ioc, outcome::failure( ... ), false, false )` → `( ..., false )` (IPFSLoader has 5 such sites); async callback capture lists drop `parse` (LocalFileLoader's explicit list; IPFSLoader's `[=]` implicit captures die automatically when the positional `parse` argument leaves the call); device-construction calls drop the parse argument: `HTTPDevice( host, path, port, save )`, `WSDevice( ... , save )`, `SFTPDevice( ... , save )` (parse was the 8th arg in SFTP — drop the right one; save stays).

  (5) DEVICE HEADERS+IMPLS (4): `HTTPCommon.hpp/.cpp`, `WSCommon.hpp/.cpp` — ctor decl+def drop `bool parse` (it is the LAST bool before save — drop the right one); delete the `bool parse_;` member and its `parse_ = parse;`/init-list initialization; ~6-8 posted-failure tails per file become single `false`; success echo `handle_read( ioc, finaldata, self->parse_, self->save_ )` → `( ioc, finaldata, self->save_ )`. `IPFSCommon.hpp/.cpp` — no member; drop `bool parse,` from `StartFindingPeers`, `StartFindingPeersWithRetry`, `RequestBlockMain`, `convertUnixFSContentToResult` (decls + defs), delete `parse, ` from every explicit capture list (research-enumerated: ~134, ~198, ~228, ~293, ~300) and every positional call/forward; the two `handle_read( ioc, outcome::..., parse, save )` echoes at ~294/301 become `( ioc, outcome::..., save )`.

  (6) SFTP MIRROR — NOT COMPILED (Discovery 2): `SFTPCommon.hpp/.cpp` and `SFTPLoader.cpp` are absent from `add_library`; MSVC will NOT check them. Mirror the HTTP/WS shapes mechanically — no improvisation: ctor drops the parse param (8th arg position), delete `parse_` member + its `parse_ = parse;` init at ~27, ~10 `handle_read` sites incl. the echo at ~400 follow the `(parse, save)`→`(save)` / `(false, false)`→`(false)` rule, scrub the comment at ~392 (`//We've read all the data, send to parse/save` — reword without "parse"). Do NOT add these files to `add_library` (dormant by design, v2 PROTO-01).

  (7) TESTS (5): `localfile_loader_test.cpp` — two call sites drop the `false, // parse` line (keep `false, // save`). `http_loader_test.cpp` (~186, ~222, ~247) and `ipfs_loader_test.cpp` (~150, ~221, ~293, ~321) — same drop. `filemanager_test.cpp` — the `OutstandingOperations` test's `false, false` → `false` AND the Plan 01 D-19 test's `false, true,` → `true,` (drop parse, keep save=true — this keeps PARSE-03's coverage live against the new signature). `ipfs_device_test.cpp` — BOTH call sites AND lambda definitions: the three `IPFSDevice::CompletionCallback` lambdas at ~78-83, ~112-119, ~176-184 change parameter lists `(ioc, ResultType, bool, bool)` → `(ioc, ResultType, bool)`; the three device calls at ~85, ~123, ~184 `StartFindingPeers[WithRetry]( runner.ioc(), cid, name, 0, false, false, callback )` keep exactly ONE `false` (the save flag). Network tests are gated ON in this environment (9 tests) so all of this is compile-verified.

  (8) EXAMPLE (D-22): `example/MNNExample.cpp` ~267-273 — delete the `false, // don't parse` line; keep `true,  // save` (reword the stale comment `// don't save - just load` to `// save` — it is wrong today and stays wrong otherwise). NOTHING else in the example changes; Phase 4 rewrites it.

  Do NOT touch `include/URLStringUtil.h`, `src/URLStringUtil.cpp`, `include/httplib.h` (legit `parse*` survivors — Discovery 1), the saver side (FileSaver.hpp, LocalFileSaver/IPFSSaver/SFTPSaver — no parse concept), or `FileManager::SaveASync` (already parse-free).

  SCOPE RULING (checker W-2): this 26-file task executes as ONE single commit — the type-coupled signature change cannot land partially-compiling (base virtual + 9 callback aliases), so green-at-every-commit holds at transaction granularity, not per-file; no split is possible or intended. Work exclusively from the PATTERNS.md excerpts in <read_first> (the pre-computed edit map) to keep executor context lean.

  Commit message: `feat(03-02): strip parse bool from every async API signature, callback, and call site (PARSE-02)`.
  </action>
  <verify>
  <automated>cmake --build build/Windows/Debug --config Debug → exit 0 (library + MNNExample + all test targets); ctest --test-dir build/Windows/Debug -C Debug --output-on-failure → "100% tests passed, 0 tests failed" (9 tests incl. LoadASync_SaveTrueAutoSavesLoadedDataToDisk)</automated>
  </verify>
  <acceptance_criteria>
  - `include/FileLoader.hpp` CompletionCallback is exactly `std::function<void( std::shared_ptr<boost::asio::io_context> ioc, ResultType buffers, bool save )>` and `LoadASync` has no parse parameter
  - All 5 loader headers declare the same parse-free alias and override; all 4 device headers are parse-free (`grep -c 'parse_' include/HTTPCommon.hpp include/WSCommon.hpp include/SFTPCommon.hpp` → 0)
  - `src/IPFSCommon.cpp` and `src/IPFSLoader.cpp` contain zero bare `parse` tokens (`git grep -nw parse -- src/IPFSCommon.cpp src/IPFSLoader.cpp` → exit 1)
  - The 3 uncompiled SFTP sites (`SFTPCommon.hpp`, `SFTPCommon.cpp`, `SFTPLoader.cpp`) contain zero parse tokens of any form (`git grep -n parse -- include/SFTPCommon.hpp src/SFTPCommon.cpp src/SFTPLoader.cpp` → exit 1; `include/SFTPLoader.hpp` too — it IS compiled but verify anyway)
  - `test/src/ipfs_device_test.cpp` lambda parameter lists are 3-param `(ioc, ResultType, bool)` and device calls keep exactly one `false`
  - `example/MNNExample.cpp` has exactly one `LoadASync(` call with no parse argument (D-22)
  - Windows build green including the `MNNExample` target and all 5 edited test targets; full ctest green (9 tests, zero failures)
  - The Plan 01 D-19 test passes against the new signature (PARSE-03 preserved)
  </acceptance_criteria>
  <done>Every signature, callback, capture list, failure tail, test call, lambda definition, and the example line is parse-free; build + all 9 tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-end verification gate — D-23 word-boundary grep + D-24 build/ctest composition</name>
  <files>.planning/phases/03-parser-layer-removal/03-02-SUMMARY.md</files>
  <read_first>
  - .planning/phases/03-parser-layer-removal/03-CONTEXT.md (D-23 pattern list as decided; D-24 gate composition — grep + build + ctest, NO runtime parse test)
  - .planning/phases/03-parser-layer-removal/03-RESEARCH.md (Discovery 1 — the false-positive analysis and word-boundary amendment; "Open Questions (RESOLVED)" — the adopted `param parse` doc-pattern tightening, zero legit survivors; A2 — pattern portability across grep tools)
  - .planning/phases/02-localfilecommon-platform-split/02-02-SUMMARY.md (the Phase 2 gate-evidence format to mirror)
  </read_first>
  <action>
  Run the D-23/D-24 gate and capture exact outputs in the SUMMARY:

  (1) D-23 narrow grep with the research-verified word-boundary patterns (mechanics amendment per Discovery 1, decision scope unchanged) plus the one live-verified vendored exclusion (httplib.h:4288 declares a method literally named `parse`, so `bool\s+parse\b` matches it — excluded as vendored, not ours):

  `git grep -nE 'bool\s+parse\b|\bparse_\b|FileParser|ParseData|RegisterParser|parsers\[|parsers\.find' -- include/ src/ test/ example/ ':(exclude)include/httplib.h'`

  Empty output / exit code 1 = PASS. Any hit is phase-blocking: fix the source (or, if a hit is in genuinely untouchable vendored code beyond httplib.h, document and exclude explicitly — never widen the patterns).

  (2) Doc-scrub tightening (free, zero legit survivors — answers RESEARCH Open Question 1): `git grep -nE '@param parse\b|param parse -' -- include/ src/` → must return nothing (~15 doc lines existed; all must be gone).

  (3) Pitfall-4 eyeball check on the capture-heavy/uncompiled four: `git grep -nw parse -- src/IPFSCommon.cpp src/IPFSLoader.cpp src/SFTPCommon.cpp src/SFTPLoader.cpp` → exit 1 (bare identifiers in capture lists are invisible to the D-23 patterns).

  (4) D-24 composition: `cmake --build build/Windows/Debug --config Debug` → exit 0; `ctest --test-dir build/Windows/Debug -C Debug --output-on-failure` → 9 tests, 0 failures (full count including the network-gated set and Plan 01's D-19 test). Record the ctest summary lines verbatim.

  (5) Cross-check the ROADMAP Phase 3 success criteria 1-5 against the evidence (FileParser deleted + registry symbols gone — Plan 01; parse-free signatures incl. tests+example — Task 1; auto-save test green — criterion 3/D-19; no behavioral change — criterion 4 via unchanged D-19 content assertions; green suite — criterion 5). Note residual caveats honestly in the SUMMARY: SFTPCommon.cpp/SFTPLoader.cpp/SFTPCommon.hpp verified by grep-only (Discovery 2 — not compiled on Windows; MSVC-green does not prove them), and the installed-package surface change (`FileParser.hpp` vanishes from the next `install/*.h*` — expected breaking change, no shims per PROJECT.md).

  Commit message: `docs(03-02): phase 3 verification gate evidence (D-23/D-24)`.
  </action>
  <verify>
  <automated>git grep -nE 'bool\s+parse\b|\bparse_\b|FileParser|ParseData|RegisterParser|parsers\[|parsers\.find' -- include/ src/ test/ example/ ':(exclude)include/httplib.h' → exit code 1; git grep -nE '@param parse\b' -- include/ src/ → exit code 1; git grep -nw parse -- src/IPFSCommon.cpp src/IPFSLoader.cpp src/SFTPCommon.cpp src/SFTPLoader.cpp → exit code 1; ctest --test-dir build/Windows/Debug -C Debug → "100% tests passed, 0 tests failed" (9 tests); Test-Path .planning/phases/03-parser-layer-removal/03-02-SUMMARY.md → True</automated>
  </verify>
  <acceptance_criteria>
  - The D-23 word-boundary grep (with the single httplib.h exclusion) over include/ src/ test/ example/ returns zero matches — raw output captured in the SUMMARY
  - The `@param parse` doc grep over include/ src/ returns zero matches
  - The bare-parse grep over the four capture-heavy files returns zero matches
  - Windows build green and ctest reports 9 tests, 0 failures — summary lines recorded verbatim
  - `03-02-SUMMARY.md` exists containing: all three grep outputs, the ctest lines, the ROADMAP criteria 1-5 cross-check, the SFTP grep-only caveat, and the installed-header-surface note
  - No runtime parse test was added (D-24 honored — `git grep -n no_parse_test -- test/` → nothing)
  </acceptance_criteria>
  <done>D-23/D-24 gate passes with captured evidence; Phase 3 closes with every ROADMAP criterion accounted for.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compile-time API surface | The signature change breaks all downstream consumers referencing parse arity — accepted breaking change (PROJECT.md: no shims) |
| vendored code | `include/httplib.h` and `URLStringUtil.*` carry legit `parse*` identifiers; the gate must not force edits there |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Repudiation | D-23 gate could pass vacuously via false-positive fatigue (loose patterns → 28 hits → executor ignores output) | mitigate | Word-boundary patterns verified against every enumerated legit survivor (Discovery 1) + the single httplib.h exclusion; three separate greps (D-23, doc, bare-parse) each with distinct blind spots |
| T-03-04 | Tampering | Uncompiled SFTP edits could silently rot (no compiler checks them) | mitigate | Mechanical mirroring of the compiled HTTP/WS shapes (Pitfall: no improvisation) + the bare-parse grep as the only automated check for those files; caveat recorded in SUMMARY |
| T-03-05 | Denial of Service | Missed arity fix in ipfs_device_test lambdas breaks the network-gated build | mitigate | Task enumerates BOTH call sites and lambda definitions; network gate is ON in this environment (9 tests) so MSVC compile-verifies them |
| T-03-SC | Tampering | package installs | accept (n/a) | Zero packages installed — pure refactor of in-tree code |
</threat_model>

<verification>
- Atomic strip: one commit; build green first try after all edits (the plan forbids partial states)
- D-23 grep (word-boundary + httplib.h exclusion): zero matches over include/ src/ test/ example/
- Doc grep (`@param parse`) and bare-parse grep (4 files): zero matches
- Full ctest: 9 tests, 0 failures, including `LoadASync_SaveTrueAutoSavesLoadedDataToDisk` (PARSE-03) and the network-gated set (compile-verification of test edits)
- SUMMARY with all gate evidence + SFTP grep-only caveat + installed-surface note
</verification>

<success_criteria>
- PARSE-02 complete: no parse parameter anywhere in the public API, callbacks, devices, tests, or example
- PARSE-03 preserved: D-19 auto-save test green after the strip, byte-equal content assertions unchanged
- PARSE-04 honored: D-19 test body needed only the arity fix (parse arg drop) — zero behavioral edits anywhere else
- D-23/D-24 gate passes with evidence; Phase 3 done
</success_criteria>

<output>
Create `.planning/phases/03-parser-layer-removal/03-02-SUMMARY.md` when done
</output>
