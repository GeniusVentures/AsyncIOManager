---
phase: 02-localfilecommon-platform-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - include/LocalFileCommon.win.hpp
  - include/LocalFileCommon.posix.hpp
  - src/LocalFileCommon.win.cpp
  - src/LocalFileCommon.posix.cpp
  - include/LocalFileCommon.hpp
  - src/LocalFileCommon.cpp
  - src/CMakeLists.txt
autonomous: true
requirements: [PLAT-01, PLAT-02, PLAT-03, PLAT-04]
must_haves:
  truths:
    - "include/LocalFileCommon.hpp is a platform-neutral umbrella: no #ifndef _WIN32 / #ifdef / #if defined(_WIN32) anywhere, and its selector line #include ASIOMGR_LOCALFILE_HEADER sits outside any namespace (D-08, D-15)"
    - "The Windows pair declares/defines LocalFileDevice on boost::asio::stream_file with behavior byte-identical to today's #else branch — no truncate flag added, string-concat logging kept, flags_ stores raw writemode (D-11)"
    - "The POSIX pair declares/defines LocalFileDevice on boost::asio::posix::stream_descriptor with write flags O_WRONLY | O_CREAT | O_TRUNC (D-12) and a destructor whose only close is file_.close( ec_ ) (D-13)"
    - "src/LocalFileCommon.cpp does not exist on disk and no CMake source list references it (Pitfall 5)"
    - "The Windows wrapper build (build/Windows/Debug, TESTING=ON) compiles LocalFileCommon.win.cpp and the full ctest suite passes with zero failures"
  artifacts:
    - path: "include/LocalFileCommon.hpp"
      provides: "platform-neutral umbrella header (PLAT-04): banner, neutral includes, outcome re-export, namespace sgns + using namespace boost::asio, then #include ASIOMGR_LOCALFILE_HEADER"
      contains: "#include ASIOMGR_LOCALFILE_HEADER"
    - path: "include/LocalFileCommon.win.hpp"
      provides: "self-contained Windows LocalFileDevice declaration (D-16): stream_file getFile(), private members without fd_; platform headers live in include/ (D-09)"
      contains: "class LocalFileDevice"
    - path: "src/LocalFileCommon.win.cpp"
      provides: "Windows LocalFileDevice ctor + Open() moved verbatim (D-11); platform sources live in src/ (D-09)"
      contains: "stream_file::read_only"
    - path: "include/LocalFileCommon.posix.hpp"
      provides: "self-contained POSIX LocalFileDevice declaration (D-16): stream_descriptor getFile(), fd_ member retained, destructor single-close (D-13)"
      contains: "posix::stream_descriptor"
    - path: "src/LocalFileCommon.posix.cpp"
      provides: "POSIX LocalFileDevice ctor + Open() moved with O_TRUNC fix (D-12)"
      contains: "O_TRUNC"
    - path: "src/CMakeLists.txt"
      provides: "if(WIN32)/elseif(UNIX) platform selection (PLAT-03) + quoted compile definition (D-10)"
      contains: "ASIOMGR_LOCALFILE_HEADER"
  key_links:
    - from: "include/LocalFileCommon.hpp"
      to: "include/LocalFileCommon.win.hpp | include/LocalFileCommon.posix.hpp"
      via: "macro-expanded include of the CMake-provided header token"
      pattern: "#include ASIOMGR_LOCALFILE_HEADER"
    - from: "src/CMakeLists.txt"
      to: "AsyncIOManager target"
      via: "target_compile_definitions PRIVATE with embedded literal quotes (TEST_CERT_DIR form)"
      pattern: "ASIOMGR_LOCALFILE_HEADER=\"\\$\\{ASIOMGR_LOCALFILE_HEADER_NAME\\}\""
    - from: "src/CMakeLists.txt"
      to: "src/LocalFileCommon.win.cpp | src/LocalFileCommon.posix.cpp"
      via: "add_library first source entry replaced by the selection variable"
      pattern: "\\$\\{ASIOMGR_LOCALFILE_SOURCE\\}"
    - from: "src/LocalFileLoader.cpp and src/LocalFileSaver.cpp"
      to: "include/LocalFileCommon.hpp"
      via: "unchanged #include \"LocalFileCommon.hpp\" lines (zero include-line changes, D-08)"
      pattern: "#include \"LocalFileCommon.hpp\""
---

<objective>
Execute the Phase 2 platform-split transaction: `LocalFileDevice` becomes two CMake-selected source+header pairs (`LocalFileCommon.win.*` / `LocalFileCommon.posix.*`), the old dual-branch files are reduced/deleted, and `include/LocalFileCommon.hpp` becomes the neutral umbrella that dispatches to the platform header via the CMake-injected `ASIOMGR_LOCALFILE_HEADER` compile definition — with the Windows build and full test suite green at the end.

This is one atomic cutover slice per PLAT-01/PLAT-02/PLAT-03/PLAT-04 and locked decisions D-08 through D-16. Tasks 1-2 create the new platform pairs as inert files (tree still builds the old way); Task 3 performs the cutover (umbrella reduction + old-file deletion + CMake selection) so the tree is never broken mid-plan.

Purpose: eliminate platform branching from the local-file device layer (boss requirement) while keeping `LocalFileLoader.cpp`/`LocalFileSaver.cpp` byte-untouched.
Output: 4 new platform files, 1 rewritten umbrella, 1 deleted source, updated `src/CMakeLists.txt`.
</objective>

## Phase Goal

**As a** platform integrator of the AsyncIOManager library, **I want to** build the local-file device layer from platform-pure source files selected by CMake, **so that** no local-file source file contains platform branching and each platform half can be read, verified, and extended independently.

*(Adapted from the ROADMAP Phase 2 goal per orchestrator guidance for refactor phases — the "vertical slice" is the working, CMake-dispatched local-file layer proven green by the existing test suite.)*

## Artifacts this phase-plan produces

New symbols (created by this plan — exclude from drift verification):
- Compile definition: `ASIOMGR_LOCALFILE_HEADER` (PRIVATE on `AsyncIOManager`, value `"LocalFileCommon.win.hpp"` on Windows / `"LocalFileCommon.posix.hpp"` on UNIX)
- CMake variables: `ASIOMGR_LOCALFILE_SOURCE`, `ASIOMGR_LOCALFILE_HEADER_NAME` (set inside the `if(WIN32)/elseif(UNIX)` block)
- First-of-its-kind pattern: platform-conditional source selection in `src/CMakeLists.txt` (PLAT-03 creates this pattern; no prior precedent exists by design)

New file paths:
- `include/LocalFileCommon.win.hpp`, `include/LocalFileCommon.posix.hpp`
- `src/LocalFileCommon.win.cpp`, `src/LocalFileCommon.posix.cpp`

Deleted file path: `src/LocalFileCommon.cpp`

Relocated (not new) symbols: `sgns::LocalFileDevice` class and its ctor/`Open()`/`getFile()` — declared per-platform now (D-16); logger tag `"LocalFileCommon"` unchanged in both halves (D-14).

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-localfilecommon-platform-split/02-CONTEXT.md
@.planning/phases/02-localfilecommon-platform-split/02-RESEARCH.md
@.planning/phases/02-localfilecommon-platform-split/02-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Windows pair as a verbatim move (D-11, PLAT-01)</name>
  <files>include/LocalFileCommon.win.hpp, src/LocalFileCommon.win.cpp</files>
  <read_first>
  - include/LocalFileCommon.hpp (the move source: the `#else` Windows class block between `#else` and `#endif`, plus the neutral prefix lines 1-26 for the skeleton)
  - src/LocalFileCommon.cpp (the move source: the `#else` Windows impl block — ctor + Open())
  - .planning/phases/02-localfilecommon-platform-split/02-PATTERNS.md (sections "include/LocalFileCommon.win.hpp" and "src/LocalFileCommon.win.cpp" — exact file skeletons already written)
  - .planning/phases/02-localfilecommon-platform-split/02-CONTEXT.md (D-11, D-14, D-16 fidelity rules)
  </read_first>
  <action>
  Create `include/LocalFileCommon.win.hpp` using the PATTERNS "File skeleton" exactly: banner comment `Header file for the LocalFileCommon (Windows)`, `#pragma once`, the five neutral includes (`<iostream>`, `<memory>`, `"boost/asio.hpp"`, `<asiomgr-logger.hpp>`, `<libp2p/outcome/outcome.hpp>`), the `namespace outcome` re-export block (failure/result/success from libp2p::outcome), then `namespace sgns` with `using namespace boost::asio;` — then move the Windows `class LocalFileDevice` block VERBATIM from the current `#else` branch of include/LocalFileCommon.hpp (the variant whose `getFile()` returns `boost::asio::stream_file &` and whose private members have NO `fd_`). Keep the class doc comment ("This class creates a FILE Device...") above the class, reworded to "(Windows)" or kept as-is. Close with `} // End namespace sgns`. Per D-14 the logger member stays `sgns::asiomgr::createLogger( "LocalFileCommon" )`.

  Create `src/LocalFileCommon.win.cpp` per the PATTERNS skeleton: banner `Source file for the LocalFileCommon (Windows)`, `#include "LocalFileCommon.hpp"`, `namespace sgns` + `using namespace boost::asio;`, then move the Windows ctor + `Open()` VERBATIM from the `#else` block of src/LocalFileCommon.cpp. Explicitly forbidden changes (D-11, RESEARCH Pitfall 3): do NOT add `stream_file::truncate`, do NOT change the `read_only` / `write_only|create` flag mapping, do NOT modernize the string-concatenation `m_logger->error( "Failed to open file (Windows): " + ec_.message() )` to `{}` placeholders, do NOT unify the `flags_`-stores-raw-writemode asymmetry with POSIX.

  These two files are INERT until Task 3 wires them — no CMake edit in this task. The tree still builds the old dual-branch way.
  </action>
  <verify>
  <automated>git grep -nE '#[ \t]*(ifdef|ifndef|if[ \t]+defined\()' -- include/LocalFileCommon.win.hpp src/LocalFileCommon.win.cpp → must be empty (exit code 1); Select-String -Path include/LocalFileCommon.win.hpp -Pattern 'stream_file' → matches; Select-String -Path src/LocalFileCommon.win.cpp -Pattern 'stream_file::read_only','stream_file::create' → both match; Select-String -Path include/LocalFileCommon.win.hpp,src/LocalFileCommon.win.cpp -Pattern 'truncate' → empty</automated>
  </verify>
  <acceptance_criteria>
  - include/LocalFileCommon.win.hpp exists, declares `class LocalFileDevice` with `boost::asio::stream_file &getFile()`, private members `file_`/`ec_`/`filename_`/`flags_` (no `fd_`), logger tag exactly `"LocalFileCommon"`, and contains zero `#ifdef`/`#ifndef`/`#if defined(` lines
  - src/LocalFileCommon.win.cpp exists, defines `LocalFileDevice::LocalFileDevice` (initializes `flags_( writemode )` raw) and `LocalFileDevice::Open` with the exact `read_only`/`write_only` + conditional `|=` `create` logic, and contains zero platform-branching lines
  - Neither file references `truncate` in any form
  - git status shows both files as new/untracked; no existing file modified in this task
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create the POSIX pair with the two locked fixes (D-12, D-13, PLAT-02)</name>
  <files>include/LocalFileCommon.posix.hpp, src/LocalFileCommon.posix.cpp</files>
  <read_first>
  - include/LocalFileCommon.hpp (the move source: POSIX-only includes inside the first `#ifndef _WIN32` block — `<fcntl.h>` and `"boost/asio/posix/stream_descriptor.hpp"` — plus the POSIX class block between `#ifndef _WIN32` and `#else`)
  - src/LocalFileCommon.cpp (the move source: POSIX ctor with the flags expression + Open() with ::open/assign/error path)
  - .planning/phases/02-localfilecommon-platform-split/02-PATTERNS.md (sections "include/LocalFileCommon.posix.hpp" and "src/LocalFileCommon.posix.cpp" — skeletons with both fixes already marked)
  - .planning/phases/02-localfilecommon-platform-split/02-RESEARCH.md (Pattern 3: Boost 1.85 verified semantics for both fixes; Pitfall 3: which ::close stays)
  </read_first>
  <action>
  Create `include/LocalFileCommon.posix.hpp` with the same skeleton as Task 1's header but banner `(POSIX)`, and two ADDITIONAL unconditional includes after the neutral five: `<fcntl.h>` and `"boost/asio/posix/stream_descriptor.hpp"` (these move out of the umbrella per D-15). Move the POSIX `class LocalFileDevice` block (the variant with `posix::stream_descriptor &getFile()` and `int fd_ = -1;` member) applying exactly ONE change per D-13: the destructor body becomes only `file_.close( ec_ );` — delete the `close( fd_ );` line (Boost 1.85 `basic_descriptor.hpp`: `close(ec)` closes the underlying descriptor "even if the function indicates an error"; the second close is a latent EBADF/double-close). The `fd_` member itself STAYS (used by `Open()`). Logger tag stays `"LocalFileCommon"` (D-14).

  Create `src/LocalFileCommon.posix.cpp`: banner `(POSIX)`, `#include "LocalFileCommon.hpp"`, `namespace sgns` + `using namespace boost::asio;`, then move the POSIX ctor + `Open()` applying exactly ONE change per D-12: the ctor's flags initializer becomes `flags_( writemode == 0 ? O_RDONLY : O_WRONLY | O_CREAT | O_TRUNC )` (adds `| O_TRUNC` so overwrite semantics are device-self-sufficient; `<fcntl.h>` already comes via the header). PRESERVE everything else verbatim: `::open( filename_.c_str(), flags_, 0666 )`, the `errno`/`system_category()` error mapping, and CRITICALLY the `::close( fd_ ); fd_ = -1;` inside the `file_.assign` error path — that ::close is error cleanup, NOT the D-13 double-close; only the destructor's close dies.

  As in Task 1, these files are INERT until Task 3.
  </action>
  <verify>
  <automated>git grep -nE '#[ \t]*(ifdef|ifndef|if[ \t]+defined\()' -- include/LocalFileCommon.posix.hpp src/LocalFileCommon.posix.cpp → must be empty (exit code 1); Select-String -Path src/LocalFileCommon.posix.cpp -Pattern 'O_TRUNC' → matches; Select-String -Path include/LocalFileCommon.posix.hpp -Pattern 'close\( fd_ \)' → empty (destructor single-close); Select-String -Path src/LocalFileCommon.posix.cpp -Pattern '::close\( fd_ \)' → matches exactly once (the assign-error path survives)</automated>
  </verify>
  <acceptance_criteria>
  - include/LocalFileCommon.posix.hpp exists, declares `class LocalFileDevice` with `boost::asio::posix::stream_descriptor &getFile()`, member `int fd_ = -1;`, includes `<fcntl.h>` and `"boost/asio/posix/stream_descriptor.hpp"` unconditionally, destructor contains `file_.close( ec_ );` and contains NO `close( fd_ )`, zero platform-branching lines
  - src/LocalFileCommon.posix.cpp exists; its ctor initializer contains the exact expression `O_WRONLY | O_CREAT | O_TRUNC`; `Open()` retains `::open` with mode `0666`, the `file_.assign( fd_, ec_ )` call, and exactly one `::close( fd_ )` inside the assign-error branch
  - git status shows both files as new/untracked; no existing file modified in this task
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Cutover — neutral umbrella (D-15), delete old cpp, CMake selection (PLAT-03/D-10), green-build gate</name>
  <files>include/LocalFileCommon.hpp, src/LocalFileCommon.cpp, src/CMakeLists.txt</files>
  <read_first>
  - include/LocalFileCommon.hpp (current state — being reduced to the umbrella)
  - src/CMakeLists.txt (current `add_library` list with `LocalFileCommon.cpp` as first entry; `target_link_libraries`; `include_directories(../include)`; trailing `if(BUILD_TESTING)` block)
  - test/src/CMakeLists.txt lines 83-85 (the proven quoted `TEST_CERT_DIR="${TEST_CERT_DIR}"` definition form to replicate)
  - .planning/phases/02-localfilecommon-platform-split/02-PATTERNS.md (umbrella target shape + CMake selection block)
  - .planning/phases/02-localfilecommon-platform-split/02-RESEARCH.md (Pattern 1/2 mechanics; Pitfalls 1, 2, 5, 6)
  </read_first>
  <action>
  Rewrite `include/LocalFileCommon.hpp` into the neutral umbrella per D-15, in this exact order: (1) banner `Header file for the LocalFileCommon`; (2) `#pragma once`; (3) ONLY the five neutral includes (`<iostream>`, `<memory>`, `"boost/asio.hpp"`, `<asiomgr-logger.hpp>`, `<libp2p/outcome/outcome.hpp>`) — the POSIX includes and their `#ifndef _WIN32`/`#endif` wrapper are removed; (4) the `namespace outcome` re-export block; (5) `namespace sgns { using namespace boost::asio; } // End namespace sgns` — closed immediately, both class blocks and both inner `#ifndef`/`#else`/`#endif` markers deleted, and the class doc comment removed (it moved to the platform headers in Tasks 1-2); (6) a short comment explaining the CMake injection (`WIN32 -> ASIOMGR_LOCALFILE_HEADER="LocalFileCommon.win.hpp"`, `UNIX -> ...posix.hpp`); (7) an undefined-macro guard using `#if !defined( ASIOMGR_LOCALFILE_HEADER )` + `#error` + `#endif` — MUST be this form, NOT `#ifndef`, to stay clean of the D-17 scan patterns; (8) the selector `#include ASIOMGR_LOCALFILE_HEADER` as the LAST line, outside any namespace. Zero `#ifdef`/`#ifndef`/`#if defined(` lines may remain anywhere in the file.

  Edit `src/CMakeLists.txt` per PLAT-03/D-10: immediately BEFORE `add_library(AsyncIOManager STATIC`, insert the selection block `if(WIN32)` → `set(ASIOMGR_LOCALFILE_SOURCE LocalFileCommon.win.cpp)` + `set(ASIOMGR_LOCALFILE_HEADER_NAME "LocalFileCommon.win.hpp")` → `elseif(UNIX)` → the posix equivalents → `endif()`. In the `add_library` list, REPLACE the `LocalFileCommon.cpp` entry with `${ASIOMGR_LOCALFILE_SOURCE}` (all other entries untouched). AFTER `target_link_libraries` (target must exist), add `target_compile_definitions(AsyncIOManager PRIVATE ASIOMGR_LOCALFILE_HEADER="${ASIOMGR_LOCALFILE_HEADER_NAME}")` — quotes literal inside the CMake string, NO backslash escaping (Pitfall 1; this is the proven `TEST_CERT_DIR` form). Keep PRIVATE (D-10): the only TUs including the umbrella are library sources — `src/LocalFileLoader.cpp:8`, `src/LocalFileSaver.cpp:14`, and the two new platform cpps; no test/example TU does (verified in RESEARCH). Do NOT touch the trailing `if(BUILD_TESTING)` block.

  Delete the old dual-branch source in the SAME transaction as the CMake edit (Pitfall 5 — configure fails if the list still names it): `git rm src/LocalFileCommon.cpp`. Do NOT touch `src/LocalFileLoader.cpp` or `src/LocalFileSaver.cpp` — their `#include "LocalFileCommon.hpp"` lines keep working unchanged (D-08, zero include-line changes).

  Then reconfigure and verify on the Phase-1-proven wrapper tree (Pitfall 2 — the wrapper gate is `TESTING`, already ON in the cache; do NOT pass `-DBUILD_TESTING`): `cmake -S build/Windows -B build/Windows/Debug`, then `cmake --build build/Windows/Debug --config Debug` (build log must show `LocalFileCommon.win.cpp` being compiled — Pitfall 6), then `ctest --test-dir build/Windows/Debug -C Debug --output-on-failure` → zero failures.
  </action>
  <verify>
  <automated>git grep -nE '#[ \t]*(ifdef|ifndef[ \t]+_WIN32|if[ \t]+defined\([ \t]*_WIN32[ \t]*\))' -- 'include/LocalFileCommon*.hpp' 'src/LocalFileCommon*.cpp' → must be empty (exit code 1); Test-Path src/LocalFileCommon.cpp → False; cmake -S build/Windows -B build/Windows/Debug → exit 0; cmake --build build/Windows/Debug --config Debug → exit 0 with LocalFileCommon.win.cpp in the compile output; ctest --test-dir build/Windows/Debug -C Debug --output-on-failure → 100% tests passed; git diff --stat src/LocalFileLoader.cpp src/LocalFileSaver.cpp → empty (untouched per D-08)</automated>
  </verify>
  <acceptance_criteria>
  - include/LocalFileCommon.hpp contains `#include ASIOMGR_LOCALFILE_HEADER` as its selector, contains `#if !defined( ASIOMGR_LOCALFILE_HEADER )` (guard form, not `#ifndef`), contains NO `class LocalFileDevice` and NO platform-branching lines
  - src/CMakeLists.txt contains `if(WIN32)` ... `elseif(UNIX)` ... `endif()` setting `ASIOMGR_LOCALFILE_SOURCE`/`ASIOMGR_LOCALFILE_HEADER_NAME`, the `add_library` list uses `${ASIOMGR_LOCALFILE_SOURCE}` in place of `LocalFileCommon.cpp`, and contains `ASIOMGR_LOCALFILE_HEADER="${ASIOMGR_LOCALFILE_HEADER_NAME}"` inside a PRIVATE `target_compile_definitions(AsyncIOManager ...)` call
  - `src/LocalFileCommon.cpp` is deleted (staged as removed) and `git grep -n "LocalFileCommon.cpp" -- src/CMakeLists.txt` returns empty
  - Wrapper reconfigure + build succeed; the build compiles `LocalFileCommon.win.cpp`; `ctest` reports zero failures (same test count as the Phase 1 end state — no tests added or lost in this plan)
  - `git diff` shows zero changes to `src/LocalFileLoader.cpp` and `src/LocalFileSaver.cpp`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure build-config/source-layout refactor — no new runtime inputs, no network, no packages. Existing boundaries (URL strings into FileManager) unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `ASIOMGR_LOCALFILE_HEADER` compile definition (build-time injection) | accept | Build-time, developer-controlled value set in reviewed CMake; not reachable at runtime |
| T-02-02 | Denial of Service | local-file device (relocated code) | accept | Behavior byte-identical per D-11 (Windows) / two narrow reviewed fixes (POSIX, D-12/D-13); no new input paths |
| T-02-SC | Tampering | package installs | accept (n/a) | Zero packages installed this phase (code/config-only refactor) — no slopcheck gate applicable |
</threat_model>

<verification>
- All five local-file files pass the zero-#ifdef grep (formal gate re-run in Plan 02; early gate green here)
- Wrapper tree reconfigure + build + ctest green with `LocalFileCommon.win.cpp` in the compile log
- `src/LocalFileLoader.cpp` / `src/LocalFileSaver.cpp` untouched (D-08)
- Umbrella selector + CMake definition linkage proven by the successful compile (macro expands to `LocalFileCommon.win.hpp` on this platform)
</verification>

<success_criteria>
- PLAT-01: Windows pair exists, zero `#ifdef` ✓ (Task 1 + grep gate)
- PLAT-02: POSIX pair exists, structurally valid (verbatim move + two verified Boost-1.85-backed fixes), zero `#ifdef` ✓ (Task 2 + grep gate)
- PLAT-03: CMake is the only selection mechanism ✓ (Task 3)
- PLAT-04: umbrella is platform-neutral with the selector outside any namespace ✓ (Task 3)
- Windows build + full test suite green ✓ (Task 3 gate)
</success_criteria>

<output>
Create `.planning/phases/02-localfilecommon-platform-split/02-01-SUMMARY.md` when done. Record the known limitation (RESEARCH Q1): `ASIOMGR_LOCALFILE_HEADER` is PRIVATE, so installed-package consumers compiling the umbrella must define the macro themselves — accepted for this milestone, PUBLIC-on-target is the future fix if export consumers appear.
</output>
