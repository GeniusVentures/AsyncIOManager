---
phase: 02-localfilecommon-platform-split
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - test/src/no_ifdef_test.cpp
  - test/src/CMakeLists.txt
autonomous: true
requirements: [PLAT-01, PLAT-02]
must_haves:
  truths:
    - "ctest lists and passes no_ifdef_test — a permanent guard that fails if any of the five local-file files ever gains platform branching (D-17)"
    - "The guard scans exactly the five D-18 files: include/LocalFileCommon.hpp, include/LocalFileCommon.win.hpp, include/LocalFileCommon.posix.hpp, src/LocalFileCommon.win.cpp, src/LocalFileCommon.posix.cpp"
    - "The phase-end git grep over those five files returns zero matches (exit code 1 from git grep = PASS)"
    - "The full suite is green on the Windows wrapper build: prior test count + 1 (no_ifdef_test), zero failures"
  artifacts:
    - path: "test/src/no_ifdef_test.cpp"
      provides: "permanent zero-#ifdef guard test (D-17): runtime text scan of the five local-file files + scanner self-check"
      contains: "LocalFileLayerHasNoPlatformBranching"
    - path: "test/src/CMakeLists.txt"
      provides: "addtest(no_ifdef_test ...) registration + LOCALFILE_INCLUDE_DIR/LOCALFILE_SRC_DIR path defines (TEST_CERT_DIR pattern)"
      contains: "addtest(no_ifdef_test"
  key_links:
    - from: "test/src/CMakeLists.txt"
      to: "test/src/no_ifdef_test.cpp"
      via: "addtest() registration (only sanctioned path) + compile-time absolute path defines"
      pattern: "LOCALFILE_INCLUDE_DIR="
    - from: "test/src/no_ifdef_test.cpp"
      to: "the five local-file files"
      via: "std::ifstream text scan at test runtime"
      pattern: "containsIfdefViolation"
---

<objective>
Install the permanent zero-#ifdef enforcement (D-17) and run the phase-end verification gate: a new `no_ifdef_test` ctest that reads the five local-file files (D-18) as text and fails on `#ifdef`, `#ifndef _WIN32`, or `#if defined(_WIN32)`, plus the one-shot phase grep gate and full-suite green confirmation on the Windows wrapper build. Optional non-blocking stretch: WSL `g++ -fsyntax-only` check of the POSIX half.

Purpose: protect the boss requirement (no platform branching in the local-file layer) from future regression — the guard outlives the phase.
Output: `test/src/no_ifdef_test.cpp`, updated `test/src/CMakeLists.txt`, phase verification evidence in the SUMMARY.
</objective>

## Phase Goal

**As a** maintainer of the AsyncIOManager library, **I want to** a permanent automated guard that fails the test suite whenever platform branching re-enters the local-file layer, **so that** the Phase 2 split (PLAT-01/PLAT-02 zero-#ifdef invariant) cannot silently regress in future phases or contributions.

*(Verification/enforcement slice of the ROADMAP Phase 2 goal; depends on Plan 01 having created the five files.)*

## Artifacts this phase-plan produces

New symbols (created by this plan — exclude from drift verification):
- Test target: `no_ifdef_test` (registered via `addtest()`, always built — NOT gated behind `ASYNC_IO_MANAGER_NETWORK_TESTS`)
- Test file: `test/src/no_ifdef_test.cpp` with `TEST( NoIfdefTest, LocalFileLayerHasNoPlatformBranching )`, `TEST( NoIfdefTest, ScannerDetectsViolations )` (self-check), and static helper `containsIfdefViolation( const std::string &path )`
- Compile definitions on the test target: `LOCALFILE_INCLUDE_DIR="${CMAKE_CURRENT_SOURCE_DIR}/../../include"`, `LOCALFILE_SRC_DIR="${CMAKE_CURRENT_SOURCE_DIR}/../../src"`

No production symbols are created or modified by this plan.

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-localfilecommon-platform-split/02-CONTEXT.md
@.planning/phases/02-localfilecommon-platform-split/02-RESEARCH.md
@.planning/phases/02-localfilecommon-platform-split/02-PATTERNS.md
@.planning/phases/02-localfilecommon-platform-split/02-01-SUMMARY.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create no_ifdef_test.cpp and register it via addtest() (D-17, D-18)</name>
  <files>test/src/no_ifdef_test.cpp, test/src/CMakeLists.txt</files>
  <read_first>
  - test/src/CMakeLists.txt (registration layout: the "Phase 2: File-based tests" section with localfile_loader_test/localfile_saver_test/filemanager_test blocks; the TEST_CERT_DIR quoted-define at lines 83-85; the ASYNC_IO_MANAGER_NETWORK_TESTS gate this test must NOT sit behind)
  - test/src/localfile_loader_test.cpp lines 1-15 (gtest include + banner style to copy)
  - .planning/phases/02-localfilecommon-platform-split/02-PATTERNS.md (section "test/src/no_ifdef_test.cpp" — full skeleton; and "test/src/CMakeLists.txt" — registration block)
  - .planning/phases/02-localfilecommon-platform-split/02-RESEARCH.md (Pattern 4 mechanics; Pitfall 4 — include-guard false positives)
  - include/LocalFileCommon.hpp (scan target: confirm the `#if !defined( ASIOMGR_LOCALFILE_HEADER )` guard line does NOT trip the three patterns)
  </read_first>
  <action>
  Create `test/src/no_ifdef_test.cpp` per the PATTERNS skeleton: banner comment stating the boss requirement and the D-17/D-18 contract; includes `<gtest/gtest.h>`, `<fstream>`, `<string>`, `<vector>`; a file-static helper `containsIfdefViolation( const std::string &path )` that opens the file with `std::ifstream`, returns true with `ADD_FAILURE() << "cannot open " << path` when it cannot open (a deleted/renamed local-file file must FAIL the guard, not skip it), and otherwise scans line by line: strip leading spaces/tabs, then the line is a violation iff the stripped line (a) starts with `#ifdef`, or (b) starts with `#ifndef` and contains `_WIN32`, or (c) starts with `#if` followed by whitespace and contains both `defined` and `_WIN32`. Exactly these three forms — `#pragma once` and any own-name include guard must NOT match (Pitfall 4); comment lines starting with `//` naturally fail the prefix test and are safe.

  Two tests: (1) `TEST( NoIfdefTest, LocalFileLayerHasNoPlatformBranching )` — builds the exact five D-18 paths from the two compile-time defines (`std::string( LOCALFILE_INCLUDE_DIR ) + "/LocalFileCommon.hpp"`, `... + "/LocalFileCommon.win.hpp"`, `... + "/LocalFileCommon.posix.hpp"`, `std::string( LOCALFILE_SRC_DIR ) + "/LocalFileCommon.win.cpp"`, `... + "/LocalFileCommon.posix.cpp"`) and loops `EXPECT_FALSE( containsIfdefViolation( f ) ) << f;`. (2) `TEST( NoIfdefTest, ScannerDetectsViolations )` — the anti-vacuity self-check: write a temp file (e.g. `no_ifdef_scanner_probe.txt` via `std::ofstream`) containing one line each of `#ifdef _WIN32`, `#ifndef _WIN32`, and `#if defined( _WIN32 )`, plus a benign `#pragma once` line; assert the helper returns true for it; `std::remove` the temp file before the test ends (RAII or explicit call so a failure path still cleans up).

  Edit `test/src/CMakeLists.txt`: in the "Phase 2: File-based tests" section, directly after the `filemanager_test` block, add `addtest(no_ifdef_test` / `    no_ifdef_test.cpp` / `)` followed by `target_compile_definitions(no_ifdef_test PRIVATE` with `LOCALFILE_INCLUDE_DIR="${CMAKE_CURRENT_SOURCE_DIR}/../../include"` and `LOCALFILE_SRC_DIR="${CMAKE_CURRENT_SOURCE_DIR}/../../src"` (quotes literal — TEST_CERT_DIR form; `CMAKE_CURRENT_SOURCE_DIR` is `test/src`, so `../../` reaches the repo root; CMake keeps forward slashes on Windows — safe for std::ifstream). NO `target_link_libraries` needed — the test only reads text files; `addtest()` already links gtest_main. Do NOT place the block inside the `if(ASYNC_IO_MANAGER_NETWORK_TESTS)` gate — the guard is always built.
  </action>
  <verify>
  <automated>cmake -S build/Windows -B build/Windows/Debug → exit 0; cmake --build build/Windows/Debug --config Debug --target no_ifdef_test → exit 0; ctest --test-dir build/Windows/Debug -C Debug -R no_ifdef_test --output-on-failure → passes (2 tests); ctest --test-dir build/Windows/Debug -C Debug -N → output lists no_ifdef_test alongside the existing suite</automated>
  </verify>
  <acceptance_criteria>
  - test/src/no_ifdef_test.cpp exists and contains `TEST( NoIfdefTest, LocalFileLayerHasNoPlatformBranching )`, `TEST( NoIfdefTest, ScannerDetectsViolations )`, and the helper `containsIfdefViolation`
  - The five-file list in the test names exactly: `LocalFileCommon.hpp`, `LocalFileCommon.win.hpp`, `LocalFileCommon.posix.hpp` under `LOCALFILE_INCLUDE_DIR`, and `LocalFileCommon.win.cpp`, `LocalFileCommon.posix.cpp` under `LOCALFILE_SRC_DIR` — no other files scanned (D-18)
  - test/src/CMakeLists.txt contains `addtest(no_ifdef_test` OUTSIDE the `if(ASYNC_IO_MANAGER_NETWORK_TESTS)` block, with the two quoted path defines in a PRIVATE `target_compile_definitions(no_ifdef_test ...)` call
  - `no_ifdef_test` passes both its tests on the wrapper build (proving the five files are branch-free AND the scanner actually detects violations)
  - No other test target or CMake block changed (git diff shows only the one added block)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Phase-end verification gate — zero-#ifdef grep + full suite + optional POSIX syntax check</name>
  <files>.planning/phases/02-localfilecommon-platform-split/02-02-SUMMARY.md</files>
  <read_first>
  - .planning/phases/01-localfile-rename-mnn-code-purge/01-02-PLAN.md (the Phase 1 grep-gate contract this gate mirrors)
  - .planning/phases/02-localfilecommon-platform-split/02-RESEARCH.md (Environment Availability table — WSL stretch check A3 details; Pitfall 2 — wrapper `TESTING` gate)
  - .planning/phases/02-localfilecommon-platform-split/02-01-SUMMARY.md (Plan 01 evidence to build on)
  </read_first>
  <action>
  Run the three gate steps and capture evidence (exact outputs) for the SUMMARY:

  (1) One-shot zero-#ifdef grep gate (D-17): run `git grep -nE '#[ \t]*(ifdef|ifndef[ \t]+_WIN32|if[ \t]+defined\([ \t]*_WIN32[ \t]*\))' -- 'include/LocalFileCommon*.hpp' 'src/LocalFileCommon*.cpp'` — empty output with exit code 1 = PASS (same semantics as the Phase 1 zero-mnn gate). Any match is a phase-blocking failure to fix before proceeding.

  (2) Full suite: `cmake --build build/Windows/Debug --config Debug` then `ctest --test-dir build/Windows/Debug -C Debug --output-on-failure` — zero failures; test count is the Plan 01 end-state count + 1 (`no_ifdef_test` with 2 gtest cases inside). Record the ctest summary lines verbatim in the SUMMARY.

  (3) OPTIONAL, non-blocking stretch (RESEARCH A3): if WSL Ubuntu-22 is available, syntax-check the POSIX half with a real POSIX compiler — `wsl -d Ubuntu-22 -- g++ -fsyntax-only -std=c++17 -DASIOMGR_LOCALFILE_HEADER='"LocalFileCommon.posix.hpp"' -I/mnt/w/<repo>/include -I<boost-1_85 parent> -I<spdlog include> -I<libp2p include> -I<fmt include> /mnt/w/<repo>/src/LocalFileCommon.posix.cpp` (resolve the thirdparty include dirs under `/mnt/w/gnus/GeniusNetwork/thirdparty/build/Windows/Debug` by locating `boost/asio.hpp`, `spdlog/spdlog.h`, `libp2p/outcome/outcome.hpp`, `fmt/core.h`; pass the macro define with embedded literal quotes). Disposition rules: success → record as bonus evidence upgrading PLAT-02 to "syntax-checked with a real POSIX compiler"; environment failure (distro missing, mount absent, headers not found) → acceptable skip, note it; GENUINE compile errors in `LocalFileCommon.posix.hpp/.cpp` → a real PLAT-02 finding: fix the POSIX files in this task, re-run steps (1) and (2). The stretch never gates phase completion on environment problems.

  Write the SUMMARY with: gate (1) output, gate (2) ctest lines, stretch result, the file inventory (five local-file files + their zero-#ifdef status), and carry-forward of the Plan 01 known limitation (PRIVATE `ASIOMGR_LOCALFILE_HEADER` → installed-package consumers must define it themselves).
  </action>
  <verify>
  <automated>git grep -nE '#[ \t]*(ifdef|ifndef[ \t]+_WIN32|if[ \t]+defined\([ \t]*_WIN32[ \t]*\))' -- 'include/LocalFileCommon*.hpp' 'src/LocalFileCommon*.cpp' → exit code 1 (no matches); ctest --test-dir build/Windows/Debug -C Debug --output-on-failure → "100% tests passed, 0 tests failed"; Test-Path .planning/phases/02-localfilecommon-platform-split/02-02-SUMMARY.md → True</automated>
  </verify>
  <acceptance_criteria>
  - The phase-end grep command over `include/LocalFileCommon*.hpp` and `src/LocalFileCommon*.cpp` returns zero matches (git grep exit code 1) — output captured in the SUMMARY
  - Full ctest run on `build/Windows/Debug` (Debug config) reports zero failures and includes `no_ifdef_test`
  - The WSL stretch outcome is recorded (pass evidence, acceptable-skip reason, or the fix + re-run if genuine POSIX errors were found)
  - `02-02-SUMMARY.md` exists and contains the grep evidence, the ctest summary lines, and the known-limitation note
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → test runtime | `LOCALFILE_INCLUDE_DIR`/`LOCALFILE_SRC_DIR` absolute paths baked at compile time from `CMAKE_CURRENT_SOURCE_DIR` (developer-controlled, not runtime input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | no_ifdef_test reads repo sources as text | accept | Paths are compile-time constants from the build tree; no untrusted input reaches the test; failures fail loud (unopenable file = ADD_FAILURE) |
| T-02-04 | Repudiation | guard could pass vacuously | mitigate | `ScannerDetectsViolations` self-check proves the scanner flags all three banned forms on a crafted probe file before trusting green results |
| T-02-SC | Tampering | package installs | accept (n/a) | Zero packages installed — test uses only in-repo GTest infrastructure via `addtest()` |
</threat_model>

<verification>
- `no_ifdef_test` registered, listed by `ctest -N`, and passing (both gtest cases)
- Phase-end zero-#ifdef grep gate empty (exit 1)
- Full suite green: prior count + 1, zero failures
- Stretch check outcome documented (non-blocking)
</verification>

<success_criteria>
- D-17 satisfied: permanent ctest guard + one-shot phase gate both green
- D-18 satisfied: scan scope is exactly the five local-file files (verified by the test's file list)
- ROADMAP criteria 1-2 (zero `#ifdef` in win/posix pairs) and 5 (green Windows suite) carry permanent enforcement from here on
</success_criteria>

<output>
Create `.planning/phases/02-localfilecommon-platform-split/02-02-SUMMARY.md` when done (per Task 2), including all gate evidence and the carry-forward known limitation from Plan 01.
</output>
