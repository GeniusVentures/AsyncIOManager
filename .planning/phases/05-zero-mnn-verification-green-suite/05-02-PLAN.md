---
phase: 05-zero-mnn-verification-green-suite
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: []
autonomous: true
requirements: [TEST-04, BUILD-02]
must_haves:
  truths:
    - 'A Release-configured verify tree exists at `build/Windows/Release` whose CMake cache holds `ASYNC_IO_MANAGER_NETWORK_TESTS:BOOL=OFF`, and `ctest --test-dir build/Windows/Release -N -C Release` reports `Total Tests: 4` — the D-33 default gate (localfile_loader_test, localfile_saver_test, filemanager_test, no_ifdef_test), NOT 9'
    - '`cmake --build build/Windows/Release --clean-first --parallel 8 --config Release` exits 0 — every object recompiled from scratch (D-39: reconfigure + `--clean-first` in the existing wrapper tree is the accepted "fresh out-of-source" proof; a virgin build dir is NOT required)'
    - '`ctest --test-dir build/Windows/Release -C Release --output-on-failure` prints `100% tests passed, 0 tests failed out of 4` (TEST-04, D-40) — 4/4 is the CORRECT expected count under the default gate; an "out of 9" line means the cache-sticky network-tests-ON configure happened and is a gate FAILURE, not more coverage'
    - 'All three Release pins are aligned in the executed commands: `-DCMAKE_BUILD_TYPE=Release` (configure) + `--config Release` (build) + `-C Release` (ctest), with `-DASYNC_IO_MANAGER_NETWORK_TESTS=OFF` passed explicitly on reconfigure (D-40 + cache-stickiness pitfall)'
    - 'The D-35 gate and TEST-03 sub-gate re-run green at phase end: `git grep -iI mnn -- include/ src/ test/ example/ CMakeLists.txt ''*CMakeLists.txt'' ''*.cmake'' ''*.CMake'' README.md` → exit 1 AND `git grep -iI \.mnn -- test/` → exit 1 (BUILD-02 final-state proof)'
    - 'Phase evidence is recorded in 05-02-SUMMARY.md: every command with its observed exit code, the ctest summary line quoted verbatim, the 4-row roadmap Phase-5 success-criteria table, the preemptive note about untracked pre-Phase-1 `mnn_*.exe` relics in `build/Windows/Release/test_bin/Release/` (gate-invisible, left per deferred hygiene), and the 4-vs-9 count explanation with the gate definition recorded alongside the count'
  artifacts:
    - path: ".planning/phases/05-zero-mnn-verification-green-suite/05-02-SUMMARY.md"
      provides: "the phase's live evidence record (D-34): configure/build/ctest command log with exit codes, ctest 4/4 summary line, both grep gates, 4-row roadmap criteria table, relic + count-explanation notes — quote-ready for mirroring into 05-VERIFICATION.md by the verification step"
  key_links:
    - from: "ctest registry in the Release verify tree"
      to: "test/src/CMakeLists.txt always-on suites (via addtest())"
      via: "exactly 4 registered tests when ASYNC_IO_MANAGER_NETWORK_TESTS=OFF — ctest -N pre-check before the 10-min build catches a sticky 9-entry registry early"
      pattern: "Total Tests: 4"
    - from: "05-02-SUMMARY.md evidence rows"
      to: "05-VERIFICATION.md (created by the subsequent verify step per D-34, not by this plan)"
      via: "commands + observed outputs quoted verbatim so the verify step lifts them without re-running the ~10-min rebuild (04-VERIFICATION trusted-evidence lineage)"
      pattern: "100% tests passed, 0 tests failed out of 4"
---

<objective>
Execute the milestone's green-suite acceptance loop live and record its evidence: reconfigure the Release verify tree with the network-tests gate explicitly OFF (D-33 default gate), confirm the 4-entry ctest registry before building, run the `--clean-first` full-recompile Release build (D-39), and pass `ctest -C Release` with 4/4 (TEST-04, D-40) — then re-run both grep gates against the final tree state and consolidate all four roadmap Phase-5 success criteria into the evidence record.

Purpose: TEST-04 requires the full always-on suite green on Windows and BUILD-02 requires the library to verifiably build clean with zero MNN dependency — this plan is the live proof for both halves, plus the durable evidence trail (D-34) the verification step mirrors into 05-VERIFICATION.md.
Output: green Release verification loop + green final-state grep gates, all evidence consolidated in `05-02-SUMMARY.md`, `05-02-SUMMARY.md`. (No tracked source files change — the verify tree stays untracked.)
</objective>

## Phase Goal

**As a** maintainer accepting the modernization milestone, **I want to** reconfigure, rebuild from scratch, and run the full always-on test suite in Release on Windows and watch every acceptance gate pass with recorded evidence, **so that** the "zero-MNN, green-suite" milestone claim rests on a live, reproducible run rather than carried-forward Debug-phase assertions.

*(Wave 2 of the milestone acceptance gate: Wave 1 delivered "README rewritten + grep gates green"; this plan delivers "Release verification loop executed + evidence recorded" — together they close TEST-03/TEST-04/BUILD-02 and all four roadmap criteria.)*

## Artifacts this phase-plan produces

New symbols (created by this plan — exclude from drift verification):
- Evidence document: `.planning/phases/05-zero-mnn-verification-green-suite/05-02-SUMMARY.md` (command log + exit codes + ctest summary line + 4-row criteria table + relic/count notes)
- Untracked build state (NOT committed — repo tracks only 6 wrapper/cmake files under `build/`): reconfigured `build/Windows/Release` tree with cache `ASYNC_IO_MANAGER_NETWORK_TESTS:BOOL=OFF`, `CMAKE_BUILD_TYPE=Release`

Downstream (NOT created by this plan): `05-VERIFICATION.md` is written by the subsequent verification step per D-34 and the PATTERNS.md file classification ("created during phase-end verification, not by execute-phase code plans") — this plan's SUMMARY is its quote-ready source.

Removed symbols: none (no source files change; contingency straggler fixes belong to 05-01 Task 2).

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-zero-mnn-verification-green-suite/05-CONTEXT.md
@.planning/phases/05-zero-mnn-verification-green-suite/05-RESEARCH.md
@.planning/phases/05-zero-mnn-verification-green-suite/05-PATTERNS.md
@.planning/phases/05-zero-mnn-verification-green-suite/05-01-SUMMARY.md
@.planning/phases/04-build-purge-generic-example/04-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Release verify loop — reconfigure (gate OFF explicit), 4-entry pre-check, --clean-first rebuild, ctest 4/4 (TEST-04, D-33/D-39/D-40)</name>
  <files>(none modified — verification; untracked build tree only; results recorded in 05-02-SUMMARY.md)</files>
  <read_first>
  - .planning/phases/05-zero-mnn-verification-green-suite/05-RESEARCH.md ("Code Examples" §1 — the exact recommended command chain; Pitfall 1 "The 9-vs-4 ctest count confusion" — expected count is 4; Pitfall 2 "CMake cache stickiness on reconfigure" — the OFF flag MUST be explicit; Pitfall 5 "MSVC multi-config flag mismatches" — all three Release pins aligned; Pitfall 8 "CMake Tools extension configure" — terminal cmake only; Pitfall 9 "Build duration" — ~10 min, no tree mutation mid-build; Assumption A2 fallback — Debug-tree-in-place if Release thirdparty libs fail to link)
  - .planning/phases/05-zero-mnn-verification-green-suite/05-PATTERNS.md (Shared Pattern 2 "Wrapper reconfigure → clean rebuild → config-pinned ctest loop" — the non-negotiables list)
  - .planning/phases/04-build-purge-generic-example/04-VERIFICATION.md (the evidence-quote style: command → observed output verbatim, trusted-evidence preamble)
  </read_first>
  <action>
  Run the verification loop from repo root in a terminal (never the VS Code CMake Tools extension — it cannot configure this super-build):

  (1) Reconfigure the Release sibling tree with the gate flag EXPLICIT (both existing build-tree caches hold `ASYNC_IO_MANAGER_NETWORK_TESTS:BOOL=ON`; omitting the flag silently keeps the wrong 9-entry gate):
  `cmake -S build/Windows -B build/Windows/Release -G "Visual Studio 17 2022" -A x64 -DCMAKE_BUILD_TYPE=Release -DTHIRDPARTY_DIR=W:\gnus\GeniusNetwork\thirdparty -DASYNC_IO_MANAGER_NETWORK_TESTS=OFF` → expect exit 0.

  (2) REGISTRY PRE-CHECK before the expensive build: `ctest --test-dir build/Windows/Release -N -C Release` → must print `Total Tests: 4`. If it prints 9, STOP — the OFF flag did not take (re-run configure with the flag and check the cache line `ASYNC_IO_MANAGER_NETWORK_TESTS:BOOL=OFF` via Select-String on build/Windows/Release/CMakeCache.txt) — do NOT proceed to a 10-minute build against the wrong gate.

  (3) Full clean rebuild: `cmake --build build/Windows/Release --clean-first --parallel 8 --config Release` → expect exit 0 (~10 min; every object recompiles — this is D-39's "clean compile from scratch" proof). Do not edit any tracked file while it runs.

  (4) Full ctest: `ctest --test-dir build/Windows/Release -C Release --output-on-failure` → expect the verbatim line `100% tests passed, 0 tests failed out of 4` (suites: localfile_loader_test, localfile_saver_test, filemanager_test, no_ifdef_test — D-33 default gate). Record the line verbatim plus the per-suite pass list.

  (5) FALLBACK (Assumption A2 — only if step 3 fails to LINK against the Release thirdparty tree at W:\gnus\GeniusNetwork\thirdparty\build\Windows\Release): reconfigure the existing Debug tree in place instead — `cmake -S build/Windows -B build/Windows/Debug -G "Visual Studio 17 2022" -A x64 -DCMAKE_BUILD_TYPE=Release -DTHIRDPARTY_DIR=W:\gnus\GeniusNetwork\thirdparty -DASYNC_IO_MANAGER_NETWORK_TESTS=OFF` — then repeat steps 2-4 against build/Windows/Debug. Both arrangements are D-39-allowed; document which one produced the evidence. Compile errors (not link errors against thirdparty) are NOT a fallback trigger — they are a real failure to fix or report.

  (6) Record in 05-02-SUMMARY.md: every command verbatim + exit code, the `Total Tests: 4` pre-check output, the ctest summary line verbatim, the suite list, which tree arrangement was used (Release sibling expected / Debug fallback), and the note that the expected count is 4 because the D-33 default gate excludes the 5 network-gated suites (Phase 4's "out of 9" evidence came from a network-ON configure — not a regression baseline).

  No tracked file changes in this task; the verify tree stays untracked (repo tracks only 6 files under build/).
  </action>
  <verify>
  <automated>cmake configure → exit 0; ctest -N → "Total Tests: 4"; cmake --build ... --clean-first --parallel 8 --config Release → exit 0; ctest --test-dir build/Windows/Release -C Release --output-on-failure → "100% tests passed, 0 tests failed out of 4"; Select-String -Path build/Windows/Release/CMakeCache.txt -Pattern 'ASYNC_IO_MANAGER_NETWORK_TESTS:BOOL=OFF' → 1 match</automated>
  </verify>
  <acceptance_criteria>
  - `build/Windows/Release/CMakeCache.txt` contains `ASYNC_IO_MANAGER_NETWORK_TESTS:BOOL=OFF` (explicitly passed, not cached-ON)
  - `ctest -N -C Release` on the verify tree prints `Total Tests: 4` — the four always-on suites, no network-gated entries
  - `cmake --build build/Windows/Release --clean-first --parallel 8 --config Release` exits 0 (or the documented Debug-tree fallback was executed with identical pins and its evidence recorded)
  - `ctest --test-dir build/Windows/Release -C Release --output-on-failure` prints `100% tests passed, 0 tests failed out of 4`
  - All three Release pins appear in the recorded commands: `-DCMAKE_BUILD_TYPE=Release`, `--config Release`, `-C Release`
  - 05-02-SUMMARY.md quotes the configure/build/ctest commands with exit codes, the `Total Tests: 4` pre-check, the ctest summary line verbatim, the suite list, and the 4-vs-9 explanation
  - `git status --short` shows no new tracked-file modifications from this task (verify tree untracked)
  </acceptance_criteria>
  <done>
  Release verification loop green: gate-OFF reconfigure exit 0, registry pre-check `Total Tests: 4`, `--clean-first` full rebuild exit 0, ctest `100% tests passed, 0 tests failed out of 4` — all quoted with exit codes in 05-02-SUMMARY.md. TEST-04 live-proven; BUILD-02's build half live-proven.
  </done>
</task>

<task type="auto">
  <name>Task 2: Phase-end consolidated gate — final-state greps + 4-row roadmap criteria table (BUILD-02/TEST-03 stamp, D-34 evidence consolidation)</name>
  <files>(none modified — verification; results recorded in 05-02-SUMMARY.md)</files>
  <read_first>
  - .planning/ROADMAP.md (Phase 5 section — the four success criteria this table discharges: zero-mnn grep incl. straggler fixes; no `.mnn` test references; green Windows suite zero ctest failures; fresh end-to-end per D-39's reconfigure + `--clean-first` definition)
  - .planning/phases/05-zero-mnn-verification-green-suite/05-01-SUMMARY.md (Wave-1 gate evidence — baseline citation + post-rewrite gate runs this task re-confirms at final state)
  - .planning/phases/05-zero-mnn-verification-green-suite/05-PATTERNS.md (the 05-VERIFICATION.md content requirements list — items 1-5 this task pre-stages in the SUMMARY; Shared Pattern 3 "Evidence formatting")
  </read_first>
  <action>
  (1) Re-run both gates at final tree state (post-README-rewrite, post-verify-build — proving nothing regressed during Wave 2), from repo root, checking `$LASTEXITCODE` after each:
  - D-35 full gate: `git grep -iI mnn -- include/ src/ test/ example/ CMakeLists.txt '*CMakeLists.txt' '*.cmake' '*.CMake' README.md` → expect exit 1, empty.
  - TEST-03 sub-gate: `git grep -iI \.mnn -- test/` → expect exit 1, empty.

  (2) Build the 4-row roadmap Phase-5 success-criteria table in 05-02-SUMMARY.md, one row per criterion with its evidence citation:
  - Row 1 — zero-mnn grep over include/ src/ test/ example/ all CMakeLists + README: TRUE, cites the D-35 gate exit-1 runs (Wave 1 + this task) and the 16-hit pre-rewrite baseline citation from 05-01-SUMMARY.md.
  - Row 2 — no test references `.mnn`, fixtures generic: TRUE, cites the TEST-03 sub-gate exit-1 run (+ the Phase-4 fixture-swap evidence: test/fixture.bin).
  - Row 3 — full suite green on Windows BUILD_TESTING=ON, ctest zero failures: TRUE, cites Task 1's `100% tests passed, 0 tests failed out of 4` verbatim line with the D-33 gate definition noted beside the count.
  - Row 4 — fresh out-of-source end-to-end per D-39: TRUE, cites Task 1's reconfigure + `--clean-first` + ctest chain (the accepted reconfigure-in-existing-wrapper-tree definition — not a virgin dir).

  (3) Preemptive-false-alarm notes (required in the record per PATTERNS 05-VERIFICATION content requirements): (a) untracked pre-Phase-1 relics `mnn_loader_test.exe` / `mnn_saver_test.exe` may still exist under `build/Windows/Release/test_bin/Release/` — untracked build artifacts, invisible to the git-grep gate, deliberately left per the deferred build-artifact-hygiene decision; `--clean-first` does not remove outputs of deleted targets. (b) the count is 4, not Phase-4's 9 — the D-33 default gate vs a network-tests-ON configure; a 9-entry registry in this phase would be a gate error, not extra coverage.

  (4) Confirm the record is quote-ready for 05-VERIFICATION.md (created by the subsequent verify step per D-34, mirroring 04-VERIFICATION.md: frontmatter → Observable Truths → Requirements Coverage → 4-row success-criteria table with quoted command output) — every evidence row already carries command + observed output verbatim.

  No tracked file changes in this task.
  </action>
  <verify>
  <automated>git grep -iI mnn -- include/ src/ test/ example/ CMakeLists.txt '*CMakeLists.txt' '*.cmake' '*.CMake' README.md → exit 1 (empty); git grep -iI \.mnn -- test/ → exit 1 (empty); Select-String -Path .planning/phases/05-zero-mnn-verification-green-suite/05-02-SUMMARY.md -Pattern '100% tests passed','Total Tests: 4','--clean-first','D-33','mnn_loader_test.exe' → every pattern ≥1 match</automated>
  </verify>
  <acceptance_criteria>
  - Both gates exit 1 with empty output at final tree state — BUILD-02/TEST-03 final-state proof
  - 05-02-SUMMARY.md contains a 4-row criteria table where each row cites its evidence (gate exit codes for rows 1-2, the verbatim ctest line for row 3, the reconfigure + `--clean-first` chain for row 4)
  - 05-02-SUMMARY.md contains the stale-relic note naming `mnn_loader_test.exe` (and `mnn_saver_test.exe`) as untracked/gate-invisible/left-by-deferred-decision, and the 4-vs-9 count explanation naming the D-33 default gate
  - `git status --short` shows no tracked-file modifications from this task
  </acceptance_criteria>
  <done>
  Phase-end consolidated gate complete: both greps green at final state, all four roadmap Phase-5 criteria recorded TRUE with evidence citations, preemptive relic + count notes in place, evidence quote-ready for 05-VERIFICATION.md. TEST-03, TEST-04, BUILD-02 all discharged across the phase.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Verification loop over the existing codebase and build system. No new code paths, inputs, or services. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Repudiation | phase evidence record | mitigate | Every gate quoted as command + observed exit code/output verbatim in 05-02-SUMMARY.md; the ctest count is recorded WITH its gate definition (D-33 default, OFF flag verified in cache) so the 4/4 result cannot be misread as lost coverage |
| T-05-04 | Tampering | untracked verify build tree | accept | Local build artifacts only (repo tracks 6 files under build/); no committed artifacts; stale mnn_*.exe relics explicitly documented as out-of-gate-scope rather than silently ignored |
| T-05-SC | Tampering | package installs | accept | Zero installs this plan (05-RESEARCH Package Legitimacy Audit: none required) |
</threat_model>

<verification>
Task 1's live Release loop (gate-OFF reconfigure → 4-entry pre-check → `--clean-first` rebuild → ctest 4/4 with all three Release pins aligned) is TEST-04's and BUILD-02's build-half proof. Task 2's final-state gate re-runs plus the 4-row criteria table close all roadmap Phase-5 criteria with citable evidence. Together with 05-01 (README + gates), the milestone acceptance gate is fully discharged; the verify step mirrors the SUMMARY evidence into 05-VERIFICATION.md per D-34.
</verification>

<success_criteria>
- All Task 1-2 acceptance criteria hold at plan end
- Release verify tree: cache `ASYNC_IO_MANAGER_NETWORK_TESTS:BOOL=OFF`, `ctest -N` → `Total Tests: 4`, `--clean-first` build exit 0, ctest `100% tests passed, 0 tests failed out of 4` (or documented Debug-tree fallback with identical pins)
- D-35 gate + TEST-03 sub-gate exit 1 at final state
- 05-02-SUMMARY.md carries the complete quote-ready evidence: commands + exit codes, verbatim ctest line, 4-row criteria table, relic note, 4-vs-9 explanation
- TEST-04 discharged; BUILD-02 discharged (with 05-01); no tracked source files modified by this plan
</success_criteria>

<output>
Create `.planning/phases/05-zero-mnn-verification-green-suite/05-02-SUMMARY.md` when done (include the full verification-loop command log with exit codes, the verbatim ctest summary line, both grep-gate runs, the 4-row roadmap criteria table, and the preemptive relic/count notes).
</output>
