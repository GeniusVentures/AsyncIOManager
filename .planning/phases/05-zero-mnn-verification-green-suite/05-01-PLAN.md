---
phase: 05-zero-mnn-verification-green-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [BUILD-02, TEST-03]
must_haves:
  truths:
    - 'The D-35 zero-mnn gate `git grep -iI mnn -- include/ src/ test/ example/ CMakeLists.txt ''*CMakeLists.txt'' ''*.cmake'' ''*.CMake'' README.md` exits 1 with empty output — zero MNN matches across the entire gate scope including the rewritten README.md (BUILD-02, D-35)'
    - 'The TEST-03 sub-gate `git grep -iI \.mnn -- test/` exits 1 — no test references `.mnn` files; all fixtures are generic binaries or self-created temp files (TEST-03)'
    - 'README.md is a full rewrite (D-36) of roughly 80-120 lines titled `AsyncIOManager` carrying zero stale-era tells: `git grep -iI -E ''mnn|pars'' -- README.md` exits 1 — no parse parameter, no RegisterParser/ParseData/FileParser tier, no MNNLoader, no mnn:// prefix, no flatbuffer section (D-37)'
    - 'README.md contains the D-38 verbatim build commands: Windows form `cmake .. -G "Visual Studio 17 2022" -A x64 -DCMAKE_BUILD_TYPE=Release -DTHIRDPARTY_DIR=W:\gnus\GeniusNetwork\thirdparty` plus `cmake --build . --parallel 8 --config Release`, POSIX make form with `-DTHIRDPARTY_DIR=/Users/fuu/gnus/thirdparty/` + `make -j8`, AND a Ninja variant (`-G Ninja` + `ninja -j8`), with a note that `-DTESTING=ON` optionally enables the test suite'
    - 'README.md documents that the wrapper lives at `build/<Platform>/`, requires a prebuilt thirdparty tree, and that the root `CMakeLists.txt` is consumed by the super-build ExternalProject rather than configured directly (D-38)'
    - 'README.md protocol/prefix table matches the live `src/FileManager.cpp` InitializeSingletons registrations (D-37): file:// load+save, https:// load, ipfs:// load+save, sftp:// save only (load dormant), wss:// load dormant — dormant handlers labeled as such, never documented as active'
    - 'README.md API sketch copies signatures from include/FileManager.hpp verbatim — ResultType and FinalCallback aliases, LoadASync(url, save, ioc, finalcall, savertype), SaveASync(url, data, ioc, finalcall, save_location = nullptr) — NO parse parameter anywhere (D-36)'
  artifacts:
    - path: "README.md"
      provides: "rewritten consumer-facing documentation of the post-refactor library: identity + URL-dispatch tagline, protocol/prefix contract table, parse-free public API sketch, dependency list resolved via the prebuilt thirdparty tree, D-38 canonical build commands, FileExample usage block"
      contains: "AsyncIOManager"
  key_links:
    - from: "README.md API sketch"
      to: "include/FileManager.hpp (ResultType, FinalCallback, LoadASync, SaveASync declarations)"
      via: "signatures copied verbatim from the live header — no documented symbol the header does not declare; outcome::result deleted-default-ctor note (std::optional capture) if shown"
      pattern: "FinalCallback"
    - from: "README.md protocol/prefix table"
      to: "src/FileManager.cpp InitializeSingletons()"
      via: "only initialized loaders/savers documented as active; SFTPLoader/WSLoader dormant status preserved"
      pattern: "sftp://"
---

<objective>
Rewrite README.md from the ground up (D-36) so the repo's only remaining MNN residue — 16 case-insensitive hits, live-enumerated at old lines 4, 16, 20, 34, 38, 66, 79-86, 97-99 — disappears as a consequence of documenting post-refactor reality (D-37 depth, D-38 verbatim build commands), then run the full D-35 zero-mnn gate and the TEST-03 sub-gate live and record their exit-code evidence.

Purpose: BUILD-02's grep gate cannot pass while README.md still carries the FileParser/MNNParser diagrams, the parsers-map FileManager diagram, the MNN GitHub dependency link, the fake MNNExample 1.mnn transcript, and the "TODO: Need to update" Windows/macOS stubs; the rewrite is the phase's only expected file edit and it converts the gate from 16-matches to exit-1 clean.
Output: fully rewritten `README.md` (zero mnn/pars strings), green D-35 gate + TEST-03 sub-gate evidence recorded in `05-01-SUMMARY.md`, `05-01-SUMMARY.md`.
</objective>

## Phase Goal

**As a** downstream consumer evaluating AsyncIOManager, **I want to** read a README that describes the library I would actually get — URL-dispatched async load/save over Boost.Asio with a prefix table, a parse-free API, and build commands that work against my prebuilt thirdparty tree — **so that** nothing in the repo suggests an ML-framework dependency or parser layer the library no longer has.

*(Wave 1 of the milestone acceptance gate: this plan delivers "README rewritten + grep gates green" — a complete, independently verifiable outcome. Wave 2 executes the Release verification loop and records the phase evidence.)*

## Artifacts this phase-plan produces

New symbols (created by this plan — exclude from drift verification):
- Rewritten document: `README.md` (full content replacement; new title heading `# AsyncIOManager`)
- New README content elements: URL-dispatch tagline; protocol/prefix table (file://, https://, ipfs://, sftp://, wss:// rows); simplified registry view (Loaders/Savers only — no parser tier; mermaid or plain table per executor discretion); dependency list naming Boost.Asio/Beast/SSL, libp2p, ipfs-lite/bitswap, libssh2, spdlog (prebuilt thirdparty tree — no fetch links) + GTest tied to `-DTESTING=ON`; D-38 canonical build command block (Windows + POSIX make + POSIX Ninja); wrapper-location note (`build/<Platform>/`, root `CMakeLists.txt` consumed by super-build ExternalProject); API sketch (`ResultType`, `FinalCallback`, `LoadASync`, `SaveASync` — no parse); FileExample usage block
- Evidence strings recorded in `05-01-SUMMARY.md`: D-35 gate exit code, TEST-03 sub-gate exit code, pre-rewrite 16-hit baseline citation

Removed symbols (deleted by this plan — all inside README.md):
- `# FileLoader` title; "Parsing and loading any format file support by Genuis Project" tagline; "MNN format" bullet; the three mermaid diagrams (FileLoader/MNNLoader, FileParser/MNNParser/IPFSParser, FileManager with `parsers` map + `RegisterParser`/`ParseData`); MNN GitHub dependency link; fake `MNNExample 1.mnn` build/run transcript; "Build with Windows / Mac OS: TODO: Need to update" stubs; "Future thinking: flatbuffer" section

Unchanged on purpose: every file other than `README.md` (live D-35 scan shows zero non-README matches — any straggler is a contingency, not a scheduled edit).

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-zero-mnn-verification-green-suite/05-CONTEXT.md
@.planning/phases/05-zero-mnn-verification-green-suite/05-RESEARCH.md
@.planning/phases/05-zero-mnn-verification-green-suite/05-PATTERNS.md
@README.md
@include/FileManager.hpp
@example/FileExample.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: README.md full rewrite — post-refactor reality, D-38 verbatim commands (D-36/D-37/D-38)</name>
  <files>README.md</files>
  <read_first>
  - README.md (current ~104-line file — the rewrite base; the old section skeleton (title/tagline → design → dependencies → build → run example) is kept as flow, all content replaced per the disposition table)
  - .planning/phases/05-zero-mnn-verification-green-suite/05-PATTERNS.md (section "`README.md` (consumer documentation, full rewrite)" — the old-section→new-content disposition table, the verbatim API signatures with header line references, the protocol table derived from InitializeSingletons, the D-38 command block, and the FileExample usage facts; Shared Pattern 4 "API-surface anti-drift")
  - .planning/phases/05-zero-mnn-verification-green-suite/05-RESEARCH.md ("README.md Rewrite Inventory" — the 16 mnn-hit old lines (4, 16, 20, 34, 38, 66, 79-86, 97-99) and section dispositions; "Code Examples" §3 API sketch, §4 protocol table, §5 FileExample facts; Pitfall 6 "README rewrite drifts from live API")
  - include/FileManager.hpp (the source of truth for every documented signature — ResultType at :56-57, FinalCallback at :69-73, LoadASync at :101-107, SaveASync at :109-115; NO parse parameter exists)
  - src/FileManager.cpp (InitializeSingletons at :29-40 — which loaders/savers are actually initialized; SFTPLoader and WSLoader initializations are commented out = dormant)
  - example/FileExample.cpp (usage comment at :1-13 and print statements at :34-38/:57-61 — the facts the usage block documents)
  </read_first>
  <action>
  Replace the entire content of README.md (D-36 — full rewrite, not a scrub) with a concise practical document of roughly 80-120 lines (D-37). Structure (order is discretion; line budgets are guidance summing to ~100):

  (1) Title + tagline (~6 lines): heading `# AsyncIOManager`; one-to-two-sentence identity — a C++17 asynchronous I/O library for loading and saving data across local and remote protocols behind one URL-dispatched `FileManager` API, built on Boost.Asio. No parsing claims (the parser layer is gone).

  (2) Protocol/prefix table (~10 lines): columns Prefix / Load / Save / Notes with exactly these rows — `file://` load+save (LocalFileLoader/LocalFileSaver); `https://` load (plain http:// registration remains disabled); `ipfs://` load+save; `sftp://` save only, load dormant; `wss://` load dormant. Dormant rows must be visibly labeled dormant (e.g. "dormant (not initialized)") — never documented as active. Optionally add the simplified Loaders/Savers registry view (mermaid or plain table, NO parser tier) at discretion if line budget allows.

  (3) Design note (~4 lines, may survive condensed from the old "Singleton pattern" section): loaders/savers are self-registering singletons dispatched by URL prefix through `FileManager::GetInstance()`.

  (4) Public API sketch (~25 lines): copy verbatim from include/FileManager.hpp — the `ResultType` alias (outcome::result of shared_ptr to pair of path-vector and byte-buffer-vector), the `FinalCallback` alias, `LoadASync( url, save, ioc, finalcall, savertype )`, `SaveASync( url, data, ioc, finalcall, save_location = nullptr )`. NO parse parameter anywhere. If the sketch shows result capture, use the `std::optional<FileManager::ResultType>` pattern (outcome::result has a deleted default constructor — 04-VERIFICATION override lineage).

  (5) Dependencies (~10 lines): Boost.Asio/Beast/SSL, libp2p, ipfs-lite/ipfs-bitswap, libssh2, spdlog — all consumed via a prebuilt thirdparty tree (no fetch/download links; state the tree requirement); Google Test when building the suite (`-DTESTING=ON`).

  (6) Build (~30 lines): the D-38 verbatim commands — Windows: `cmake .. -G "Visual Studio 17 2022" -A x64 -DCMAKE_BUILD_TYPE=Release -DTHIRDPARTY_DIR=W:\gnus\GeniusNetwork\thirdparty` then `cmake --build . --parallel 8 --config Release`, noting `-DTESTING=ON` optionally builds the test suite. POSIX (make): `cmake .. -DCMAKE_BUILD_TYPE=Debug -DTHIRDPARTY_DIR=/Users/fuu/gnus/thirdparty/` then `make -j8`. POSIX (Ninja — MUST be included): `cmake .. -G Ninja -DCMAKE_BUILD_TYPE=Release -DTHIRDPARTY_DIR=...` then `ninja -j8`. State plainly: the wrapper lives at `build/<Platform>/` and requires a prebuilt thirdparty tree; the root `CMakeLists.txt` is what the super-build's ExternalProject consumes — not for direct configuring.

  (7) Run the example (~15 lines): document `FileExample [input-url] [output-dir-url]` — defaults `file://example_data.bin` (relative to CWD — run from `example/`) and `file://example_output/`; expected output lines `Loaded "<path>" (N bytes) from <url>` then `Saved "<path>" (N bytes) to <url>`; exit 0 on success, 1 on failure delivered via callback (never thrown).

  Drop entirely: the flatbuffer "Future thinking" section, all fake build/run transcripts, all "TODO: Need to update" stubs, every mermaid diagram containing FileParser/MNNParser/parsers-map content.

  Hard content gates while writing (self-check before committing): zero case-insensitive `mnn`; zero case-insensitive `pars` (covers parse/ParseData/parsers/FileParser); no `RegisterParser`; no `mnn://`; every signature byte-matched against include/FileManager.hpp; every prefix claim matched against src/FileManager.cpp InitializeSingletons.

  Commit: `docs(05-01): rewrite README.md for post-refactor zero-MNN reality (D-36/D-37/D-38)`.
  </action>
  <verify>
  <automated>git grep -iI mnn -- README.md → exit 1; git grep -iI -E 'pars' -- README.md → exit 1; (Get-Content README.md).Count -ge 75 -and (Get-Content README.md).Count -le 135 → True; Select-String -Path README.md -Pattern 'AsyncIOManager','Visual Studio 17 2022','W:\gnus\GeniusNetwork\thirdparty','/Users/fuu/gnus/thirdparty/','-G Ninja','ninja -j8','make -j8','cmake --build . --parallel 8 --config Release','LoadASync','SaveASync','FileExample','file://','https://','ipfs://','sftp://','wss://' → every pattern ≥1 match</automated>
  </verify>
  <acceptance_criteria>
  - `git grep -iI mnn -- README.md` returns no matches (exit 1) — all 16 pre-rewrite hits gone
  - `git grep -iI -E 'pars' -- README.md` returns no matches (exit 1) — no parse parameter, no RegisterParser/ParseData/FileParser/MNNParser references, no parsers-map diagram
  - `(Get-Content README.md).Count` is between 75 and 135 (D-37's ~80-120 with rounding tolerance)
  - README.md contains `# AsyncIOManager` as the title heading
  - README.md contains all five D-38 command substrings: `Visual Studio 17 2022`, `W:\gnus\GeniusNetwork\thirdparty`, `/Users/fuu/gnus/thirdparty/`, `-G Ninja`, `ninja -j8`, plus `make -j8` and `cmake --build . --parallel 8 --config Release`
  - README.md contains the wrapper note elements: `build/Windows` (or `build/<Platform>` equivalent) and `THIRDPARTY_DIR`, and states the root CMakeLists is consumed by the super-build ExternalProject
  - README.md protocol table contains all five prefixes `file://`, `https://`, `ipfs://`, `sftp://`, `wss://` with `sftp://` load and `wss://` marked dormant — no dormant handler documented as active
  - README.md API sketch contains `LoadASync` and `SaveASync` with no parse-looking parameter, and the `FinalCallback` alias name appears
  - README.md usage block contains `FileExample` and the output-line shapes `Loaded "` and `Saved "`
  - No file other than README.md is modified (`git status --short` shows only README.md after the commit)
  </acceptance_criteria>
  <done>
  README.md fully rewritten (D-36): ~80-120 lines, titled `AsyncIOManager`, zero `mnn`/`pars` strings, protocol table matched to live registrations, parse-free API sketch matched verbatim to include/FileManager.hpp, D-38 verbatim build commands for Windows + POSIX make + POSIX Ninja, real FileExample usage. Committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Live D-35 zero-mnn gate + TEST-03 sub-gate — BUILD-02 evidence (D-35/D-35b)</name>
  <files>(none modified — gate execution; contingency straggler fixes only if the gate unexpectedly fails; results recorded in 05-01-SUMMARY.md)</files>
  <read_first>
  - .planning/phases/05-zero-mnn-verification-green-suite/05-RESEARCH.md (Pattern 1 "Negation grep gate with exit-code semantics"; Pitfall 3 "git grep exit-code semantics inverted for negation gates"; Pitfall 4 "Stale mnn binaries in the Release tree trigger false alarm"; Pitfall 7 "Gate-scope creep via wrong grep pathspecs"; "Code Examples" §2 — the exact gate commands and expected results)
  - .planning/phases/05-zero-mnn-verification-green-suite/05-PATTERNS.md (Shared Pattern 1 — the D-35 command with explicit pathspecs; the conditional straggler-fix protocol)
  - .planning/phases/04-build-purge-generic-example/04-VERIFICATION.md (Behavioral Spot-Checks — the evidence-row style: command + observed exit code quoted)
  </read_first>
  <action>
  Run both gates from the repo root in a terminal (exit-code semantics: exit 1 + empty output = PASS; exit 0 + listed matches = FAIL):

  (1) D-35 full gate: `git grep -iI mnn -- include/ src/ test/ example/ CMakeLists.txt '*CMakeLists.txt' '*.cmake' '*.CMake' README.md` — expected: exit 1, empty output, after Task 1's rewrite. Verify with `$LASTEXITCODE` in PowerShell.

  (2) TEST-03 sub-gate: `git grep -iI \.mnn -- test/` — expected: exit 1, empty output (verified clean at research time; this run is the formal evidence).

  (3) CONTINGENCY (not expected — live research scan found zero non-README matches): if gate (1) exits 0 listing any file other than README.md, that straggler is fixed in-phase per roadmap Phase-5 criterion 1's explicit instruction — smallest possible diff following the offending file's own conventions (Phase-4 minimal-diff scrub pattern: one-token renames / whole-block deletions), then re-run the gate. Any straggler edit becomes an additional file in this plan's commit record and must be itemized in 05-01-SUMMARY.md. Do NOT widen the pathspecs beyond D-35's list.

  (4) Scope guard (D-35b): do NOT include `.planning/**`, `.claude/CLAUDE.md`, `.planning/codebase/*.md`, `.git/`, or build output trees — they legitimately mention MNN (historical record of removing it) and are excluded by design. If `build/Windows/Release/test_bin/Release/mnn_loader_test.exe` / `mnn_saver_test.exe` relics are observed on disk, leave them — they are untracked pre-Phase-1 artifacts, invisible to git grep, and build-artifact hygiene is explicitly deferred.

  (5) Record in 05-01-SUMMARY.md: both gate commands with their observed exit codes, plus the pre-rewrite baseline citation for the delta narrative (16 mnn hits in README.md at old lines 4, 16, 20, 34, 38, 66, 79-86, 97-99 — from 05-RESEARCH.md; do not re-derive). This evidence is re-run and quoted formally in 05-VERIFICATION.md by plan 05-02.

  No tracked file changes in this task unless contingency (3) triggers.
  </action>
  <verify>
  <automated>git grep -iI mnn -- include/ src/ test/ example/ CMakeLists.txt '*CMakeLists.txt' '*.cmake' '*.CMake' README.md → exit 1 (empty); git grep -iI \.mnn -- test/ → exit 1 (empty); $LASTEXITCODE -eq 1 after each</automated>
  </verify>
  <acceptance_criteria>
  - The D-35 gate command exits 1 with empty output — zero matches across include/, src/, test/, example/, root CMakeLists.txt, all other CMakeLists.txt, cmake/ .cmake/.CMake files, build/CommonBuildParameters.cmake, and README.md
  - The TEST-03 sub-gate `git grep -iI \.mnn -- test/` exits 1 with empty output
  - If any straggler was found and fixed: the gate re-runs to exit 1 and the fix is itemized (file + minimal diff description) in 05-01-SUMMARY.md
  - 05-01-SUMMARY.md quotes both gate commands with observed exit codes and cites the 16-hit pre-rewrite baseline (old README lines 4, 16, 20, 34, 38, 66, 79-86, 97-99)
  - No pathspec outside the D-35 set was grepped; no untracked build-tree artifact was deleted (relics left in place per D-35b + deferred hygiene)
  </acceptance_criteria>
  <done>
  BUILD-02's grep obligation is live-proven green: D-35 gate exit 1, TEST-03 sub-gate exit 1, evidence + baseline citation recorded in 05-01-SUMMARY.md. Gate re-run and formal VERIFICATION.md record happen in plan 05-02.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Documentation rewrite + read-only grep gates over tracked files. No new code paths, inputs, or services. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | README build instructions | mitigate | D-38 verbatim user-canonical commands only (no invented vcpkg/fetch flows); acceptance gate asserts each command substring; README states the prebuilt-thirdparty-tree prerequisite so copy-paste fails fast, not silently |
| T-05-02 | Information Disclosure | README content | mitigate | Commands contain only user-dictated local filesystem paths (W:\gnus\..., /Users/fuu/...); no credentials, tokens, or hostnames — confirm no secrets in final text before commit |
| T-05-SC | Tampering | package installs | accept | Zero installs this plan (05-RESEARCH Package Legitimacy Audit: none required) |
</threat_model>

<verification>
Task 1's content gates (zero mnn, zero pars, D-38 substrings, live-API match) plus Task 2's live gates (D-35 exit 1, TEST-03 sub-gate exit 1) together prove BUILD-02's grep half and TEST-03 at Wave 1. The green-suite half (TEST-04) and the formal VERIFICATION.md record (D-34) are plan 05-02's Wave-2 scope — this plan's evidence lands in 05-01-SUMMARY.md first.
</verification>

<success_criteria>
- README.md fully rewritten per D-36/D-37/D-38: ~80-120 lines, `# AsyncIOManager`, zero mnn/pars strings, live-matched protocol table and API sketch, D-38 verbatim Windows + POSIX make + POSIX Ninja commands, real FileExample usage
- D-35 gate: exit 1, empty; TEST-03 sub-gate: exit 1, empty — both quoted with exit codes in 05-01-SUMMARY.md alongside the 16-hit baseline citation
- BUILD-02 (grep half) and TEST-03 discharged; no file other than README.md touched (absent straggler contingency)
</success_criteria>

<output>
Create `.planning/phases/05-zero-mnn-verification-green-suite/05-01-SUMMARY.md` when done (include both gate commands + exit codes, the 16-hit baseline citation, and any contingency straggler fixes).
</output>
