---
phase: 01-localfile-rename-mnn-code-purge
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - include/FileLoader.hpp
  - include/FileManager.hpp
  - include/HTTPCommon.hpp
  - include/HTTPLoader.hpp
  - include/IPFSCommon.hpp
  - include/IPFSLoader.hpp
  - include/SFTPCommon.hpp
  - include/SFTPLoader.hpp
  - include/WSCommon.hpp
  - include/WSLoader.hpp
autonomous: true
requirements: [MNN-05]
must_haves:
  truths:
    - "git grep -iI mnn -- include src test ':(exclude)test/src/filemanager_test.cpp' returns empty (zero output, exit code 1)"
    - "git grep -iI mnn -- include src test hits ONLY test/src/filemanager_test.cpp (the deliberate mnn:// rejection-test strings)"
    - "The full file-based ctest suite (filemanager_test, localfile_loader_test, localfile_saver_test) is green on the Windows wrapper build after the scrub (comment-only edits cannot break compilation, but the gate proves it)"
  artifacts:
    - path: "include/IPFSCommon.hpp"
      provides: "scrubbed header — the QmNnoo bootstrap CID false-positive line deleted"
      contains: "asiomgr::createLogger" # file remains a live header; mnn-absence enforced by the truths above
  key_links: []
---

<objective>
Complete MNN-05: scrub every MNN reference from doc comments in the 10 surviving protocol headers, including the `QmNnoo` bootstrap-CID grep false-positive in include/IPFSCommon.hpp. Then run the full Phase-1 verification gate: scoped zero-grep (the exact criterion-5 contract) plus the complete file-based ctest run on the Windows wrapper build.

Purpose: MNN-05 is the requirement that makes success criterion 5 pass under the orchestrator-resolved grep contract; the full-suite ctest run closes criterion 4's primary path (wrapper TESTING=ON) and criterion 3.
Output: 10 comment-scrubbed headers, verified zero-grep state, green ctest, and Phase-1 completion evidence.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-localfile-rename-mnn-code-purge/01-CONTEXT.md
@.planning/phases/01-localfile-rename-mnn-code-purge/01-RESEARCH.md
@.planning/phases/01-localfile-rename-mnn-code-purge/01-PATTERNS.md
@.planning/phases/01-localfile-rename-mnn-code-purge/01-01-SUMMARY.md
@.planning/codebase/TESTING.md
</context>

## Artifacts this phase-plan produces

No new symbols, classes, targets, or files. This plan only:
- Rewords doc comments in 10 existing surviving headers (comment-only edits)
- Deletes the commented-out `QmNnooDu7bfjPFoTZYxMNLWUQJyrVwtbZg5gBMjTezGAJN` bootstrap line (or the whole dead commented bootstrap list, include/IPFSCommon.hpp:209-216) — already commented out, zero behavior risk
- Produces verification evidence (grep output, ctest output)

Drift verification should treat all symbols created by Plan 01 (LocalFileLoader, LocalFileSaver, LocalFileDevice, localfile_*_test targets, LocalFileLoaderTest/LocalFileSaverTest fixtures, SaveASync_MnnPrefixThrows) as existing, not new.

<tasks>

<task type="auto">
  <name>Task 1: Scrub MNN doc comments from the 10 surviving protocol headers</name>
  <files>include/FileLoader.hpp, include/FileManager.hpp, include/HTTPCommon.hpp, include/HTTPLoader.hpp, include/IPFSCommon.hpp, include/IPFSLoader.hpp, include/SFTPCommon.hpp, include/SFTPLoader.hpp, include/WSCommon.hpp, include/WSLoader.hpp</files>
  <read_first>
  - Each of the 10 headers listed in <files>, before editing it
  - .planning/phases/01-localfile-rename-mnn-code-purge/01-RESEARCH.md ("Doc-Scrub Inventory" / Pattern 3 section — verified file:line hit list)
  - .planning/phases/01-localfile-rename-mnn-code-purge/01-PATTERNS.md (Doc-Scrub Inventory table with exact current-text fragments)
  </read_first>
  <action>
  Comment-only edits — do NOT touch any code, signature, include line, or the `parse` parameter (it survives until Phase 3 per D-05). Work through the verified hit list:
  1. include/FileLoader.hpp lines 25, 35, 41: drop the "(for MNN)" parenthetical from `@param parse - ...` doc (param itself stays); change example URL `ipfs://testme.mnn` → `ipfs://testfile.bin`.
  2. include/FileManager.hpp lines 67, 90, 94, 101: drop "(for MNN)" parentheticals (×2); `".mnn", ".jpg"` → `".bin", ".jpg"`; `"mnn", "file"` → `"https", "file"` and the matching `mnn://xxxxx` example → `https://xxxxx`.
  3. include/HTTPCommon.hpp lines 53, 64: drop "(for MNN)" / "(for MNN currently)" parentheticals.
  4. include/HTTPLoader.hpp lines 33, 40-42, 48: "Load Data on the MNN file" → "Load data from the file"; "MNN file part" → "file part"; "Interpreter of MNN file" → generic equivalent.
  5. include/IPFSCommon.hpp lines 66, 112, 137: drop the "(for MNN...)" parentheticals ×3. CRITICAL at lines ~209-216: delete the commented-out libp2p bootstrap address `QmNnooDu7bfjPFoTZYxMNLWUQJyrVwtbZg5gBMjTezGAJN` — its base58 substring `mNn` matches `grep -ri mnn` (research-verified false positive, not an MNN reference, but must die for zero-grep). Delete just that one commented line, or the whole dead commented bootstrap list block if it reads cleaner — zero behavior risk either way since already commented out.
  6. include/IPFSLoader.hpp lines 56, 63-65, 72: same wording trio as HTTPLoader.
  7. include/SFTPCommon.hpp lines 39, 64: drop "(for MNN...)" parentheticals.
  8. include/SFTPLoader.hpp lines 29, 36-38, 45: same wording trio.
  9. include/WSCommon.hpp lines 51, 62: drop "(for MNN...)" parentheticals.
  10. include/WSLoader.hpp lines 16, 35, 42-44, 51: "parsing the information in an MNN model file" → generic equivalent ("parsing the information in the loaded file"); plus the same trio wording fixes.
  Suggested standard wording: `@param parse - Whether to parse file upon completion` (drop the parenthetical entirely; the parameter itself lives until Phase 3 — D-05 boundary). Line numbers are from the research snapshot; locate by text fragment if drift occurred.
  </action>
  <verify>
  <automated>git grep -iI mnn -- include src test ":(exclude)test/src/filemanager_test.cpp" — MUST print nothing (exit code 1); then git grep -iI mnn -- include src test — hits appear ONLY in test/src/filemanager_test.cpp</automated>
  </verify>
  <acceptance_criteria>
  - The exclude-form grep command exits 1 with zero output lines
  - The unexcluded grep's hit list contains exactly one file: test/src/filemanager_test.cpp
  - `git diff` shows comment-only changes across the 10 headers (no code-line changes; the `parse` parameter and all signatures intact)
  - include/IPFSCommon.hpp no longer contains the string `QmNnoo`
  - No test was deleted or renamed to satisfy the grep (warning sign from research: deleting/renaming the rejection test violates criterion 1/D-06)
  </acceptance_criteria>
  <done>Scoped zero-grep state achieved with the single sanctioned exception file; scrub was comment-only</done>
</task>

<task type="auto">
  <name>Task 2: Phase-1 verification gate — full file-based ctest + evidence capture</name>
  <files>(no file edits — build-tree run and evidence capture only)</files>
  <read_first>
  - .planning/codebase/TESTING.md (run commands; Windows multi-config ctest invocation)
  - .planning/phases/01-localfile-rename-mnn-code-purge/01-RESEARCH.md (Pitfall 3 — TESTING vs BUILD_TESTING gate; verification commands section)
  </read_first>
  <action>
  Run the final verification gate on the Windows wrapper build (primary, proven topology per orchestrator constraint — the wrapper's test gate is the `TESTING` variable, NOT root `BUILD_TESTING`; passing both gates double-adds test/ and errors):
  1. Reconfigure: `cmake -S build/Windows -B build/Windows/Debug -DTESTING=ON` (reuses the existing tree; stale mnn_* projects are regenerated away).
  2. Build everything: `cmake --build build/Windows/Debug --config Debug`.
  3. List tests: `ctest --test-dir build/Windows/Debug -C Debug -N` — confirm the registered names include filemanager_test, localfile_loader_test, localfile_saver_test and NO mnn_* entries.
  4. Run the suite: `ctest --test-dir build/Windows/Debug -C Debug --output-on-failure` — 100% pass required (network suites, if configured ON in the cache, may skip via GTEST_SKIP on missing deps — skips are acceptable, failures are not; ASYNC_IO_MANAGER_NETWORK_TESTS stays at its cached value, do not flip it).
  5. Capture evidence into the SUMMARY: the ctest summary line, both grep command outputs (empty exclude-form; single-file unexcluded form), and the list of deleted MNN files (e.g. `git log --diff-filter=D --name-only --oneline -1` or `Test-Path` results).
  OPTIONAL STRETCH (≤15 min budget, abandon fast on failure): one root-path scratch configure `cmake -S . -B build/root-check -DBUILD_TESTING=ON` with `CMAKE_PREFIX_PATH` pointing at the sibling thirdparty build outputs to close RESEARCH assumption A2. If it fails fast, record A2 as closed-negative in the SUMMARY and move on — the wrapper path already satisfies criterion 4's intent.
  </action>
  <verify>
  <automated>ctest --test-dir build/Windows/Debug -C Debug --output-on-failure — exit code 0 with zero failed tests; ctest -N shows localfile_loader_test/localfile_saver_test/filemanager_test registered and zero mnn_* tests; both git grep commands behave per Task-1 contract</automated>
  </verify>
  <acceptance_criteria>
  - ctest exit code 0; "100% tests passed" (or passed+skipped with zero failures); zero mnn_* entries in `ctest -N` output
  - `build/Windows/Debug/test_bin/Debug/localfile_loader_test.exe`, `localfile_saver_test.exe`, `filemanager_test.exe` all exist
  - Evidence (ctest summary + both grep outputs) recorded in 01-02-SUMMARY.md
  - The stretch root-path configure result (attempted or skipped-with-reason) is recorded
  </acceptance_criteria>
  <done>Phase 1 verified end-to-end: full file-based suite green on Windows wrapper build, scoped zero-grep holds, deletion list confirmed, evidence captured</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source tree → verification gates | the grep contract is the tamper-evident seal on the MNN purge; ctest seals behavior |
| Build tree → ctest registration | stale untracked build artifacts could register stale mnn_* tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | grep gate evasion (deleting/renaming the rejection test to "clean" the grep) | mitigate | acceptance criteria forbid it; the single sanctioned exception file is enumerated (test/src/filemanager_test.cpp per D-06) |
| T-01-05 | Tampering | stale build-tree test registration | mitigate | ctest -N name-list assertion catches mnn_* stragglers; reconfigure regenerates targets |
| T-01-06 | Repudiation | phase completion claims | mitigate | evidence capture into SUMMARY (ctest summary line + grep outputs + deletion list) |
| T-01-SC | Tampering | package installs | accept (N/A) | zero package installs — comment edits and build runs only |
</threat_model>

<verification>
Phase-1 gate (all must hold):
1. `ctest --test-dir build/Windows/Debug -C Debug --output-on-failure` → exit 0, zero failures
2. `ctest --test-dir build/Windows/Debug -C Debug -N` → localfile_* + filemanager_test registered, no mnn_*
3. `git grep -iI mnn -- include src test ":(exclude)test/src/filemanager_test.cpp"` → empty (exit 1)
4. `git grep -iI mnn -- include src test` → hits only test/src/filemanager_test.cpp
5. Deleted-on-disk check: `Test-Path include/MNNCommon.hpp, include/MNNLoader.hpp, include/MNNSaver.hpp, src/MNNLoader.cpp, src/MNNSaver.cpp, test/base_mnn_test.hpp` → all False
</verification>

<success_criteria>
- Success criteria 1-3 and 5 of ROADMAP Phase 1 verified TRUE with captured evidence; criterion 4 satisfied via the primary wrapper path (root BUILD_TESTING path recorded as stretch outcome)
- All Phase-1 requirement IDs (FILE-01..04, MNN-02/04/05, TEST-01/02) closed across Plans 01+02
</success_criteria>

<output>
Create `.planning/phases/01-localfile-rename-mnn-code-purge/01-02-SUMMARY.md` when done
</output>
